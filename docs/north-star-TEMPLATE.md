# Template — North Star enriquecido por tenant (`north-star-<cliente>.md`)

> Plantado 14-jul-2026 desde PAQUETE-EJECUCION §8 (Kevin #005 + DXr-YrAE_vY: valores + métricas + deadline + pivots muertos). El juicio anclado de T2 se vuelve más filoso cuanto mejor sea el North Star contra el que juzga.

```
# North Star — <cliente>
OBJETIVO ÚNICO: <qué> para <DEADLINE dd/mm/aaaa>   ← sin fecha es deseo, no meta
MÉTRICAS QUE LO MIDEN (máx 3): <m1> · <m2> · <m3>
VALORES / GUARDRAILS: qué NO se hace aunque acerque al objetivo
   (p. ej.: no crecer a costa de la paz del dueño — lente Satori)
DECISIONES DESCARTADAS (pivots muertos — NO re-proponer):
   <fecha> · <qué se descartó> · <por qué>
AL CUMPLIRSE: se celebra + se RE-FIJA el siguiente (con fecha) en el mismo acto.
Última revisión: <fecha>
```

Aplica también al propio: "4/6 clientes pagos" necesita deadline (sugerencia a validar: 31/10/2026).
