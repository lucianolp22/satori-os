node_modules/
.DS_Store
*.log
# PURGA #8: el scriptId es identificador de despliegue; no se versiona.
# Purga E2-1 B2: '*' cubre .clasp.json.personal.bak de la migración a Workspace.
.clasp.json*
# Credenciales clasp viven en ~/.clasprc.json (global), nunca en el repo.
# Secretos de API → Script Properties en GAS, jamás en archivos.
.env.local
client_secret*.json
__pycache__/
*.pyc
.venv/
*.bak
voz/web/vendor/
.env
_*.txt
_design-archivo/
scratch-b4/

# Config local de Claude Code (por maquina, nunca al repo)
.claude/settings.local.json

# Higiene 27-jul: pesados y scratch FUERA del repo (GitHub rechaza archivos >100MB;
# los .mov de pruebas suman ~1GB). El commit c29ddb9 que los arrastro se deshizo.
*.mov
*.zip
VIDEOS PRUEBA/
Grabaci*
_to_delete/
_akasha_e3/three.r128.min.js
Avatares Satori/avatares-satori-512/
*.jpeg
SPACE UNIVERSE 2.jpeg

# TC-5 · las transcripciones que baja `_charla_pull.sh` son la conversación ENTERA de cada
# cliente: lo más sensible que produce el sistema. Se generan para volcarlas al `.md` del Hilo
# en el Mac y NO se versionan — un repo con las charlas adentro es una fuga esperando un push.
entregables/charlas/

# _edificio/_partes.py: intermedio generado por extraer.py (~300KB de base64). Se regenera solo.
_edificio/_partes.py

# F2 runner (estado runtime local — NUNCA se commitea; el marcador es un gate per-maquina)
voz/runner/.encargos_runner_enabled
voz/runner/.runner_state.json
entregables/encargos/
voz/runner/.pending_reports.jsonl
voz/runner/.bak-*

# Bastión (R11, 27-ago): claves privadas. Faltaban — .clasp.json/.env.local/client_secret ya estaban.
*.pem
*.key
*.p12
