# ADENDA E3 — 17/07/2026 · GATE E2 PASADO → ejecutar E3 COMPLETO (la Oficina entera, sin versiones reducidas)
**Complementa a `ENCARGO-CODE-AKASHA-E2E3-2026-07-16.md` (sección E3 + matriz de persistencia). Esta adenda manda sobre cualquier ambigüedad.**

## Gate E2: PASADO (17-jul)
HUD verificado por Luciano en /dev: **100 fps desktop · r128 · toggle OK**. El chequeo iPhone/context-loss queda ABSORBIDO en el gate E3 (se prueba sobre la Oficina real, no sobre el andamio). **El spike es ANDAMIO y se REEMPLAZA COMPLETO en E3:** quitar `akBuild_` mínimo y el HUD del spike; CONSERVAR lo ya probado (pill puerta, overlay `#akasha`, guard `!(AK&&AK.on)` del cmLoop, patrón salida dispose+forceContextLoss+replaceWith, fail-closed sin THREE/reduced-motion).

## PRIORIDAD DE LUCIANO (17-jul, decisión de producto — NO negociable)
Al abrir la pill «⟶ Akasha», la Oficina debe verse **INDISTINGUIBLE de `AKASHA-prototipo-E113.html`** (md5 `a3f754ae646ce07357e4f91a083b66cf`, confirmado spec vigente hoy), con DATOS REALES en lugar del mock: fondo photobg del CM (reusar `.photobg/.galaxy/.veil`, NO duplicar) · Núcleo geodésico + eclipse + constelación interna + tilt/sweep/twinkle · anillo de estaciones con spin 95s + spokes + labels + cámara que persigue · Espacios de Cliente con torre de datos + «◷ Historial del Espacio» · Muelle de Decisiones · cockpit completo (Atril/Brief+lazo, Estado, Actividad, Cartera, Tareas, Aprobaciones, Calendario — tableros que navegan/flyTo) · topbar Akasha · minimapa · doble toque→voz · Vista Plana · accesos OV. **El prototipo ES la spec: se PORTA, no se rediseña ni se reduce.**

## Verificado por Cowork hoy (te ahorra el recon)
Las 15 funciones GAS de la matriz EXISTEN todas en `src/`: datosCliente · datosHoy · registrarFeedback · recomendacionesAbiertas · aprobacionDesdeRecomendacion · estadoSalud · listaClientes · tableroTareas · crearTareaQuick · resolverAprobacionUI · estadoAgentes · dispararAgenteUI · agendaRango · cerebroGrafo · capturar. **Cero endpoints nuevos.** Árbol quieto en `87347ef`.

## Orden sugerido de la sesión
1. Portar CSS del prototipo scopeado bajo `#akasha` + DOM completo + motor E113 entero, tomando `window.THREE` r128 (decisión E2: quitar el `import()` r184; las 30 APIs ya están verificadas contra el build real).
2. `DATA` mock → wiring de la matriz, una función por vez. **Mock JAMÁS en el CM real:** si una fuente falla, sección vacía con aviso discreto (fail-closed), nunca datos simulados.
3. Donde el CM ya tiene modal/flujo (tablero Tareas, calendario mensual, Bandeja, resolverAprobacionUI), Akasha lo INVOCA — un solo dueño por función. Los botones del Muelle llaman al flujo real de Aprobaciones (default-deny, confirmación).
4. Calibración visual r128: el additive blending compone en sRGB → ajustar opacidades/glow A OJO contra el prototipo (screenshots lado a lado) hasta parear; el +22% de brillo global del E113 es parte de la spec.
5. Verificación: node --check + harness del motor + selfTest() completo en editor + screenshots lado a lado (prototipo vs /dev) ANTES del eyeball.
6. Cierre: HANDOFF + commit + **git push a GitHub** (backup B3 — incluye los pendientes `a8b95ff`/`87347ef`).

## Gate E3 (Luciano, en /dev)
Lado a lado contra el prototipo (indistinguible salvo datos) · fps fluidos desktop y ≥30 iPhone PWA · 5 toggles Despacho↔Akasha sin pantalla negra y el orbe del CM retoma · una aprobación real operada desde el Muelle · Vista Plana accesible. La promoción a /exec la decide SOLO Luciano.

## Cola DESPUÉS de E3
`ENCARGO-CODE-metrica-desde-CM-2026-07-17.md` (v2, en raíz — métrica desde el CM + números SIEMPRE en cifras) → luego E4 (Pulso del Día · Sato co-presenta · Modo Enfoque · Bitácora · Eco).
