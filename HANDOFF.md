# HANDOFF — Satori OS — 2026-07-23 (espejo vivo)

> Estado vigente arriba. Todo lo que sigue del `<!-- HISTÓRICO` para abajo es archivo — no es el estado de hoy.
> Cierre completo del 20-jul (declaración formal, qué incluye / qué queda abierto): `HANDOFF-2026-07-20-CIERRE-T1-NS-UI.md`.

## Estado vigente (23-jul)

## ⛓ CADENA INTEGRAL F0→F6 — CONSTRUIDA Y PUSHEADA A /dev · GATES HUMANOS PENDIENTES

**Reporte completo (leer ESO antes que nada): `REPORTE-CADENA-2026-07-21.md`.** Encargo:
`ENCARGO-CODE-CADENA-INTEGRAL-2026-07-21.md`. Marco: `PLAN-INTEGRAL-SATORI-OS-v3-2026-07-21.md`.
Autorizaciones: **A1=SÍ · A2=NO · A3=SÍ · A4=SÍ · A5=SÍ · A6=SÍ**. **`/exec` NO se tocó.**

| Fase | Qué entró | Commit |
|---|---|---|
| **F1** | T3-M3 memoria caliente/fría · M4 golden-set de evals · M5 verificación ≥2 dominios | `9ce5e4f` |
| **F2** | T3-H: SOUL (D11) · panel de Salud humano · cerebroNodo E3.5 · mapa neural (OFF) | `209bbce` |
| **F3** | TC-W3 conectores generalizados: mapa por Config + 4 adapters por SGIC (todos OFF) | `ceb4664` |
| **F4** | TC-W1/W2/W4 Hilo end-to-end: hoja + endpoint + espejo + estación + lazo con Dirección | `bf640b2` |
| **F5** | T7 correo — **solo spec** (`docs/SPEC-correo-T7.md`), A2=NO: cero código, manifiesto intacto | (con F6) |
| **F6** | B8-código (Bucket B de la purga B5: #6/#8/#9/#10) · borrador RGPD · REPORTE · este handoff | (este) |

**Verificado offline: `node --check` de los 25 `.js` + del bloque inline de `index.html` · harness
`vm` con stubs de GAS **236/236 verdes** (41 F1 · 61 F2 · 100 F3+B8+purga · 34 F4) · guardia de drift verde
antes de cada push · CAPABILITIES regen.** Asserts nuevos en `selfTest`: **D21 · D22 · D23 · D24 ·
D25 · D26** (D27 reservada para T7). **NADA se ejecutó dentro de GAS** — ese es el riesgo real de
haber batcheado 6 fases y está declarado en el §7 del reporte.

### Lo que la cadena encontró sola (no estaba en el encargo)
1. **🐞 Bug de producción, arreglado:** `normalizarCifrasTexto_` no tenía `\b` en la alternación y se
   comía prefijos de palabras — *"cincuenta mil unidades"* → **`"50.001idades"`** (cifra inventada +
   palabra mutilada, en texto dictado por voz que iba a la hoja). Lo cazó el golden-set M4 a los diez
   minutos de existir. `selfTest` estaba verde: verificaba que no reventara, no que decidiera bien.
2. **🐞 Drift en SOUL, cerrado:** el espejo en `agent.py` corrió el sentido de S7 y S8 al adaptar la
   2ª persona. Ahora el espejo es **verbatim** y el harness compara las 8 reglas enteras.
3. **🔓 Seguridad en proyectos vecinos (reportado, NO tocado):** tokens de acceso **en claro** en
   documentación versionada de `Vehemence/DOC-Vehemence-ERP-sistema.md` §13 y `DAM/HANDOFF.md`.
   **Rotarlos y purgarlos del historial de git.**

### ⛔ Lo que corre Luciano (orden exacto en §6 del reporte)
1. **`selfTest()` en el editor** (trae las 6 tandas nuevas). `clasp run` sigue bloqueado.
2. **Eyeball guiado en /dev** — el guion pantalla-por-pantalla está en **§5 del reporte** (8 pasos:
   Salud humana · Hilo en CM y en Akasha · detalle de nodo · mapa neural · voz · brief).
3. **`sembrarConectoresHallados()`** (deja los 3 nuevos APAGADOS) → por cada uno:
   `probarConector()` → **comparar totales contra la fuente** → `encenderConector()`. Uno por uno.
4. **`repararCerebro()`** + **`repararHilo()`** en los clientes existentes.
5. **N1: correr la skill `hilo-de-trabajo`** en DAM + Vehemence y espejar con `_hilo_sync.sh`.
   Hasta entonces la vista del Hilo está vacía **a propósito**.
6. **Decidir `cerebro_map`** (mapa neural) mirando los fps en el iPhone — no es medible desde Code.

### Flags que nacieron APAGADOS (la cadena construye, la revisión enciende)
`conector_CLI-001_on` (MesaQuince) · `conector_CLI-003_on` (LC Travel) · `conector_CLI-004_on` (DAM)
· `cerebro_map` (mapa neural). Cómo encender cada uno: §3 del reporte.

### 🔴 Purga integral de Cowork (23-jul) — 3 parches aplicados sobre la cadena
1. **CRÍTICO — `id_cliente` cruzados en la siembra de conectores.** El barrido A3 confirmó los
   Spreadsheet-ID pero no leyó la hoja `Clientes`, así que los ids se pusieron por suposición:
   CLI-004 (DAM) apuntaba a la DB de **MesaQuince** y CLI-005 (SIP, sin SGIC) a la de **DAM**.
   Corregido al roster real (CLI-001 MesaQuince · CLI-003 LC Travel · CLI-004 DAM; CLI-005 fuera) y
   **fijado con asserts D25g..g6**: cruzarlo de nuevo pone rojo el selfTest.
2. **+11 gates `_soloOwner_`** en funciones de mantenimiento que mutaban Config, secretos u hojas de
   todos los tenants (`probarConector`, `encenderConector`, `apagarConector`,
   `sembrarConectoresHallados`, `estadoConectores`, `espejarHiloCSV`, **`espejarHilo`**,
   `repararHilo`, `sembrarExpirySecretos`, `rotarSecretoVoz`, `rotarSecretoOficina`).
   No entran en `ENDPOINTS_UI` — no son client-callable, son de editor.
3. **REPORTE §2.3 corregido:** Vehemence y DAM **no tienen git**; la acción es rotar el `OWNER_TOKEN`
   de Vehemence y sacar el valor del DOC, no purgar un historial que no existe.

### IDs de SGIC hallados (A3) — origen archivo:línea en §4 del reporte
LC Travel `1_5fyi…igzc` ✓ · MesaQuince `16scXu…o-d8` ✓ · DAM `1_pkEG…0dWY` ✓ · Vehemence ya cableado.
**EJF: NO hallado** (vive solo en su Script Property `FIGUERAS_SS_ID`; además su SGIC no tiene libro
contable). **Oficina Virtual: no aplica** (Python+SQLite, no Sheets). **No se adivinó ningún ID.**

**Frenar acá:** la purga integral la corre Cowork sobre este reporte + los diffs, ANTES de la sesión
de revisión. No promover a `/exec` hasta después de esa sesión.

---

## T3 · MÓDULO M (motor) — M1+M2 CONSTRUIDOS, gates de editor PENDIENTES

Encargo: `ENCARGO-CODE-T3-modulo-M-motor-2026-07-21.md`. **Pusheado a /dev**, **/exec NO se tocó**. M3-M5 y Módulo H = roadmap, NO arrancados.

**M1 · hardening de seguridad (cierra la purga de S).**
- **M1a** — `_ctxSistema_()` ya NO es bypass a ciegas (`22_seguridad.js`): solo enciende el flag de sistema si la ejecución NO tiene un usuario distinto del owner. Criterio PURO en `_ctxSistemaPermitido_(who, owner)` (trigger email='' u owner califican; un tercero NO). No rompe voz (doPost = sin usuario activo, llega tras validar el secreto) ni triggers (corren sin usuario o como el owner). **Supuesto verificado:** en este deployment `Session.getActiveUser().getEmail()` bajo trigger da `''` o el owner, nunca un tercero (queda para el eyeball de voz+triggers).
- **M1b** — `_nuevoSecreto_` con CSPRNG: `Utilities.getUuid()×2` sin guiones → 48 hex (~192 bits), reemplaza `Math.random()`. No cambia la interfaz de `rotarSecreto*`.
- **M1c** — regla anti-drift en `CLAUDE.md` (Seguridad): endpoint client-callable nuevo → alta en `ENDPOINTS_UI` en el MISMO commit.
- Asserts nuevos: **D19b4/b4b/b4c** (ctx-sistema puro), **D19d8/d8b** (secreto fuerte).

**M2 · D6 encadenado (de foto a película).**
- **M2a** — serie temporal del North Star de SISTEMA: hoja dedicada **`NS_serie`** (`[fecha, metrica, actual, meta]`) en el MAESTRO. `registrarNorteDelDia_()` persiste UN punto/día, **idempotente por fecha** (re-correr el mismo día actualiza, no duplica). Soporte = hoja dedicada (NO `cerebro_log`, que es por-tenant y Satori no es tenant; NO Config, que guarda la definición). Cableado en `corridaDiaria` (fail-silenciosa). Decisión de idempotencia aislada en `_puntoSerieAccion_` (puro, aserible sin escribir).
- **M2b** — la Tendencia del brief lee la serie (`_serieNorte_` → `_tendencia_`): con ≥2 puntos muestra el delta real; con <2 mantiene el texto honesto (respeta D14e — sin 2 puntos NO inventa).
- **M2c** — no toca `northStarSatori_` ni la siembra; D3/D18 intactos.
- Asserts nuevos: tanda **D20** (idempotencia por fecha · tendencia con 2 puntos da delta · con 1 punto null · schema de `NS_serie`).

**Verificado offline:** `node --check` de los 5 `.js` tocados ✓ · **harness vm con stubs: 11/11** (ctx-sistema 3 casos · secreto ≥40 hex y varía · idempotencia agrega/actualiza/día-nuevo · tendencia +2 acelerando · 1 punto null) ✓ · guardia drift repo↔GAS: GAS HEAD == repo salvo exactamente los 5 archivos tocados ✓ · CAPABILITIES regen ✓ · **`clasp push` a /dev HECHO**.

### ⛔ Lo que corre Luciano (2 líneas)
1. **En el editor GAS:** `selfTest()` completo (trae **D19b4** ctx-sistema puro, **D19d8** secreto fuerte, y la tanda **D20** de la serie). `clasp run` sigue bloqueado → el gate es de editor.
2. **Eyeball en /dev:** (a) **voz + un trigger siguen vivos** con el nuevo `_ctxSistema_` (Sato consulta y captura; `corridaDiaria`/`drenarCola` corren) — es la prueba de que M1a no cerró ninguna puerta legítima; (b) **la Tendencia del brief**: tras 2 `corridaDiaria` en días distintos aparece el delta real. Para verlo YA sin esperar 2 días, sembrá 2 puntos de prueba en `NS_serie` (`2026-07-20` actual=N, `2026-07-21` actual=M) y abrí el brief de sistema: la línea "Tendencia" debe decir acelerando/frenando/estable con el delta, no "aún sin ≥2 puntos".

**Frenar acá:** M1+M2 cerrados a nivel código+offline; faltan los gates de editor + eyeball. NO arrancar M3-M5 ni Módulo H (encargo aparte). La purga de M la corre Cowork.

---

## T3 · MÓDULO S (seguridad del motor) — CONSTRUIDO, gates de editor PENDIENTES

Encargo: `ENCARGO-CODE-T3-modulo-S-seguridad-2026-07-21.md`. Archivo nuevo: **`src/22_seguridad.js`** (S1-S4 juntos, blast radius chico). **NO se tocó `/exec`.**

**S1 · gate de identidad.** `_soloOwner_(nombre)` en **26 endpoints** client-callable (los 9 del encargo + los 17 que salieron del grep parseado de `google.script.run` en `index.html` y del puente `gas()/pedir()` de Akasha): 20 en `08_webapp.js`, 5 en `18_direccion.js`, `capturar` en `17_bandeja.js`. El gate va **sobre la función real**, no sobre un wrapper (un wrapper deja la original igual de llamable). El criterio de puerta vive UNA vez en `_puertaOwner_(who, owner)` (puro) y `doGet` ahora lo reusa.
- **Cómo no se rompen triggers ni voz:** 9 de esos endpoints los llaman también doPost y los triggers. Cada entrada de sistema declara contexto con `_ctxSistema_()` en su primera línea — en doPost **después** de validar el secreto (un request sin auth nunca obtiene contexto). Wrapeadas: `corridaDiaria`, `drenarCola`, `clasificarBandeja`, `chequeoLivianoDirector`, `correrDirector`, `backupSemanal`, `sincronizarConectores` + las 2 ramas de `doPost`. El flag es **por ejecución** (GAS levanta un V8 nuevo por invocación): no hay fuga entre usuarios.
- UI: `satNoAuth(e)` en los failure handlers de disparar-agente, resolver-aprobación (CM y cockpit), `showError` y el puente de Akasha → toast «Sesión no autorizada…», sin crash y sin `no_autorizado` en crudo.

**S2 · credencial con vencimiento.** `VOZ_SECRET_EXPIRA` / `OFICINA_SECRET_EXPIRA` (ISO). doPost corta con `secret_expirado` por el mismo camino fail-closed que `unauthorized`. **Decisión explícita: property ausente = NO expira** (compat — el día del deploy la voz sigue andando); el scan lo marca `warn`, no `crit`. Rotación: `rotarSecretoVoz()` / `rotarSecretoOficina()` generan 48 chars, setean expiry a +90d y **muestran el secreto UNA vez en el Registro** — el reparto NO se automatiza: Luciano lo pega a mano en el `.env` del agente y reinicia ese proceso.

**S3 · matriz de riesgo.** `riesgo_*` en Config (6 tipos) + choke point único `gateRiesgo_(tipo, ctx)`, cableado en `encolarAgente`, `ejecutarTarea_` (la cola re-decide con la matriz VIGENTE, no la de cuando se encoló) y `accionVoz_`. Siembra: lecturas `permitir` · escrituras de tenant `aprobar` (pasan por Aprobaciones) · `accion_externa`/`tocar_config`/`tocar_secretos` `bloquear` · `ejecutar_agente` `permitir` (bloquearlo mataría el «Despertar agente» del CM). **Default-deny con alcance declarado:** tipo no listado en `RIESGO_TIPOS` = bloquear siempre; valor con typo en la hoja = bloquear; tipo conocido cuya fila aún no existe en Config = default de código `RIESGO_SIEMBRA` (idéntico a la siembra de `CONFIG_DEFAULTS`, aserido en D19e8) — si no, entre el `clasp push` y el `setup()` habría una ventana con los agentes muertos en prod. No reemplaza el default-deny de Umbrales (montos): aquél decide *cuánto*, éste *qué clase de cosa*.

**S4 · security-scan.** `securityScan_()` = chequeo **7** de `correrSalud` (0 API): endpoints sin gate (introspección por `Function.toString`, lista mantenida `ENDPOINTS_UI`) · secretos vencidos/sin fecha · properties críticas vacías · kill switch legible · hojas sensibles visibles (solo en `full`, abre Sheets). Si el scan revienta, el chequeo sale `crit` (nunca «ok»).

**Verificado offline (lo que se puede sin GAS):** `node --check` de los 22 `.js` + del bloque inline de `index.html` ✓ · **harness en `vm` con stubs de GAS: 42/42 verdes** (puerta en las 4 combinaciones · `_soloOwner_` tira con otro usuario y pasa con contexto de sistema · introspección distingue gateado/no-gateado · expiry puro · matriz: Config pisa el default, typo = bloquear, Config == código en los 6 tipos · scan: detecta el endpoint sin gate, secreto vencido = crit, `credencial_por_vencer` a ≤7d, property vacía = crit, todo en orden = ok · rotación no filtra el secreto en el retorno) · **cruce estático: 26 endpoints declarados = 26 gateados, 0 faltantes, 0 duplicados** ✓ · CAPABILITIES regen ✓.

### ⛔ Bloqueos y lo que corre Luciano (2 líneas)
0. ~~**`clasp` perdió permiso**~~ → **RESUELTO (21-jul, re-login de Luciano).** Guardia de drift corrida: el HEAD de GAS era **idéntico al repo pre-T3** (`HEAD~1`), 0 ediciones hechas en el editor → **`clasp push` a /dev HECHO y verificado** (pull de vuelta: GAS == repo HEAD, 24 archivos incl. `22_seguridad.js`). **`clasp run selfTest` sigue bloqueado** («Unable to run script function») → los gates D19 quedan para el editor. **/exec NO se tocó.**
1. **En el editor GAS:** `selfTest()` (trae la serie **D19**: puerta · cobertura de los 26 endpoints · expiry · matriz default-deny · el scan detectando un endpoint sin gate sembrado a propósito · Salud con 7 chequeos). Si sale ❌ **D19b**, la Session del editor no entrega el email: mirar ESO antes que cualquier otro rojo. Después, **una vez**: `sembrarExpirySecretos()` (siembra las 2 fechas a +90d; idempotente, no pisa).
2. **Eyeball en /dev (después del `clasp push`):** el CM opera normal con los gates puestos — Hoy carga, se despierta un agente, se resuelve una aprobación, Akasha abre — y **la voz sigue andando** (Sato consulta y captura).

**Nota honesta de cobertura:** `selfTest()` invoca `doPost()` con secreto válido más arriba, y eso deja el contexto de sistema encendido; D19 lo apaga y lo restaura para no dar un verde mentiroso. El rechazo a un usuario REAL no-owner no es aserible desde GAS (no se puede falsear `Session`) — por eso el criterio vive en `_puertaOwner_`, que sí se asera exhaustivamente (y el harness offline lo prueba con Session stubbeada).

**Frenar acá:** la purga del módulo S la corre Cowork. (M1+M2 del Módulo M ya se construyeron sobre esto — ver la sección de arriba.)


**Prod:** **PROMOTE a /exec HECHO** el 21-jul 08:18 por Luciano — commit `5f8846e`, `main == origin/main`. Rollback anotado en `_promote_rollback.txt`. Eyeball de /exec: **pendiente de Luciano**. Reinicio del agente de voz: **pendiente**.

**Esta sesión (21-jul, ronda 3 · `ENCARGO-CODE-ronda3-overlays-snapshot-2026-07-21.md`) — hecho en `src/index.html`:**

1. **Overlays desde Akasha — CAUSA RAÍZ ENCONTRADA Y CORREGIDA (era un selector muerto).** El fix E3.15 se apoyaba en `#centro.ak-modal #akasha{display:none}`, pero **`#akasha` NO cuelga de `#centro`**: vive FUERA a propósito (comentario del markup ~línea 1055 — adentro le robaría las `.eyebrow` al `ccCard` del CM). O sea: **la regla nunca machó con nada**. El JS ponía la clase bien; el CSS no podía verla. Por eso E3.14 y E3.15 "no pegaron": no había nada que pegar. Verificado parseando el DOM del archivo (ancestros reales: `akasha > cosmos > gl`, `centro > kscrim`, `centro > calscrim`).
   - **Fix:** la clase de modo modal se pone **también sobre `#akasha`** y la regla que apaga la pintura es `#akasha.ak-modal{display:none}` (misma especificidad que `#akasha.on`, gana por orden). `#centro.ak-modal` se conserva porque lo leen las guardas de JS (Escape, entrada auto-saneada) y se limpia en `entrar()`/`salir()`/`modal(false)` igual que antes.
   - **Descartadas por lectura del código, no por corazonada:** las 4 puertas SÍ pasan por el handler ruteado (`acc.abrirCalendario`/`acc.abrirTablero`/`calAbrir`/`cmBoardOpen` → `akModal_`), y el canvas 3D (`#gl`) SÍ vive dentro de `#akasha`. Las ramas 1 y 2 del árbol de lectura del handoff del 20-jul quedan cerradas.
2. **Instrumentación de overlays, detrás de flag (default OFF).** `akOvlLog_(puerta)` loguea puerta · `className` de `#centro` · `display` computado de `#akasha` · canvas DENTRO/FUERA, con `console.log` **y** toast (Luciano no abre consola). Se prende con `?ovldebug=1` en la URL o `AK_DEBUG_OVL=true` en consola. Las 4 puertas van con nombre: `P1 mini-calendario del Atril`, `P2 card openBoard del Despacho`, `P3 Abrir tablero (panel TAREAS)`, `P4 día del mini-calendario`.
3. **Bug lateral corregido:** el mini-calendario del Atril acumulaba **un listener por corrida de `renderDocks()`** (hasta 3 por entrada: snapshot + ola fresh + ola final) → un click abría el calendario N veces. Guarda `_wired`, igual que `sel-cliente`.
4. **Delay al volver de la Voz — CORREGIDO:** `cmSnapGuardar`/`cmSnapLeer` pasaron de **`sessionStorage` a `localStorage`**. `voz.html` es otra navegación de `window.top`, así que el sessionStorage del Despacho moría en el camino; Akasha no tenía el delay porque su `ak_snap_v1` ya era localStorage. Contrato `{t,d}` y `maxMin` intactos — el TTL (10 min en los 3 llamadores) sigue siendo lo que decide, no la vida de la pestaña. Claves migradas: `cm_est`, `cm_salud`, `cm_hoy` — payloads del propio dueño del CM (doGet ya gateado por `OWNER_EMAIL`), sin secretos.

**Ya no figura como pendiente:** la fila test "Objetivo de prueba" (OBJ-0001) de CLI-001 **fue borrada** (hoja `objetivos` de CLI-001 con solo header, verificado).

**Sigue abierto (del cierre 20-jul):** purga de cierre de tanda + declaración formal · re-siembra de objetivos de tenants (**pausa clientes vigente**) · cola T3 (motor profundo/seguridad) · badges 3D con glifo en vez de avatar real.

**Gates:** `selfTestF2` + `selfTest` completos VERDES 2× el 20-jul (17:41 y 19:21) — **NO re-flaggear**. Esta ronda solo tocó `src/index.html` (UI); verificación offline: `node --check` del bloque `<script>` = OK.

**Cierre de la ronda:** guardia de drift repo↔GAS **OK** (solo difería `index.html`) · **`clasp push` a HEAD (/dev) OK** · commit **`23f2180`** pusheado a GitHub (`main == origin/main`). **`clasp run` sigue bloqueado** ("Unable to run script function"), así que el **`selfTest()` completo queda para Luciano en el editor GAS** — esta ronda tocó SOLO `src/index.html` (UI, cero backend). **/exec no se tocó.**

### Lo que tiene que eyeballear Luciano en /dev (2 líneas)
1. **Overlays:** desde Akasha abrir por las 4 puertas (mini-calendario del Atril · card Tablero del Despacho · "Abrir tablero →" del panel TAREAS · día del mini-calendario) — el universo 3D debe DESAPARECER detrás del overlay, y al cerrar volver intacto y sin reboot.
2. **Retorno de voz:** Despacho → Hablar con Sato → volver — el Despacho debe repintar instantáneo (como Akasha), sin delay ni velo largo.

<!-- HISTÓRICO desde acá ══════════════════════════════════════════════════════ -->

## Histórico — HANDOFF al 2026-07-17

PRÓXIMO PASO (17-jul tarde — **E3.7 boot-2tiempos EN /dev, ESPERANDO TU EYEBALL CON CRONÓMETRO EN /exec**):
**Abrí /exec** (NO /dev: /dev recompila HEAD en cada carga y miente los tiempos) y en la consola corré **`AK_T.tabla()`** tras cargar. Mirá: (a) **umbral:visible** (el motor 3D arranca con el fondo+anillos+Núcleo vacío) debe ser ~TTFP, no +15s; (b) **universo:poblado** (estaciones/Espacios/Muelle con datos) <3s warm / <6s cold; (c) **final:poblado** (docks+clima); (d) en 2ª visita en tu Mac, **snapshot:pintado** ~instantáneo. Criterio del pedido: "no pasar de 3s para todo definitivo" — juzgable SOLO en /exec warm; el piso duro es el TTFP de GAS (~2-3s) que no controlamos. Si algún tramo no llega, mandame el desglose de `AK_T.tabla()` y digo dónde quedó el cuello.
**GATES QUE FALTAN (tuyos, en el editor GAS — clasp run está bloqueado):** correr **`selfTestF2()`** (incluye **D17i**: bootUniverso/bootResto fail-closed por sección) y **`selfTest()`** completo UNA vez. **La promoción a /exec la decidís SOLO vos.**

**QUÉ HACE E3.7 (commit de esta tanda):**
- **A · arranque en 2 tiempos (esqueleto-primero):** el motor 3D se construye con un DATA VACÍO **antes** de llamar al server → fondo + umbral (flythrough) + anillos + Núcleo vacío desde el TTFP (la "vista difuminada/preview" que pediste). Cuando vuelven los datos, `engine.poblarUniverso(DATA)` agrega estaciones/Espacios/Muelle/constelación **sobre el esqueleto ya montado**, idempotente (cada `poblar*` limpia lo suyo antes de reconstruir; el teardown+rebuild es atómico en un tick → sin flicker). El umbral corre DURANTE la carga (tiempo productivo).
- **B · bootUnico → 2 llamadas GAS PARALELAS:** `bootUniverso()` (estadoAgentes + listaClientes = lo que puebla la escena) y `bootResto()` (datosHoy + recomendaciones + agenda + **estadoSalud, lo más caro, FUERA del camino de poblar**). El wall-clock es el más lento de las 2, no la suma. **Compartidas por Akasha Y el CM** (promesas memoizadas `bootUniversoP`/`bootRestoP`): 2 idas al server en total, no 3. `bootUnico` queda como suma para compat/D17h.
- **C · snapshot warm (tu decisión: payload COMPLETO, TTL 10min, localStorage):** 2ª visita en tu máquina pinta universo+docks del último-bueno AL INSTANTE (marca "actualizando…"), y el fresh pisa cuando llegan las 2 olas. Bastión: dato real tuyo, autenticado, TTL corto, el fresh siempre gana. (Sabido: la **constelación** del snapshot se re-streamea con la 3ª ola de cerebros — el resto es instantáneo; es un detalle de warm-load, no bloquea.)
- **D · velo honesto:** el velo dark "Cargando el CM…" (`#cmVeil`, z-80) se interponía entre el photobg del CM y el overlay de Akasha (por eso la nebulosa tardaba ~18s). Ahora **`veloFade()` lo saca apenas hay esqueleto**, sin esperar el boot del CM. Failsafe del velo bajado 12s→4s. `ccLimpiarManiquies` intacto.

**VERIFICADO OFFLINE (harness `_akasha_e3/` contra el r128 REAL, SRI verificado):** motor construido en **0ms** (sin esperar server, esqueleto-primero) · universo poblado en 15ms vs docks en 413ms (`RESTO_MS=400`) → **universo ANTES que docks** ✓ · 3ª ola: Núcleo 0→11 nodos (reg por Espacio correcto) · **D17j: `poblar` 2×/3× NO duplica meshes** (counts idénticos: 24 hits/6 est/4 cli/3 streams/2 tarjetas/11 nodos) · **snapshot: 2ª entrada pobló 6 agentes sin esperar server** · 5 toggles, dispose+forceContextLoss 7/7, AK.on=false al salir, **0 console.error** · `node --check` de los 3 archivos · parser HTML limpio (0 cierres sueltos, `<style>` 2/2) · guardia diff repo↔GAS: solo difieren index.html + 08_webapp.js + 09_selftest.js (lo esperado). **Lo que el harness NO mide: fps y los SEGUNDOS reales — eso es tu gate en /exec.**

**Nota de asserts:** D17i vive en `selfTestF2` (server, GAS-runnable). **D17j vive en el harness** (`_akasha_e3/harness.js`), no en selfTestF2: prueba `engine.poblar` que necesita Three+DOM, imposible server-side. El gate "selfTestF2 con D17i/j" se cumple con D17i en el editor + D17j en el harness (ya verde).

**Commits de la cadena:** E3.1 `8aad9f4` · E3.2 `6b68c03` · E3.3 `1b8707c` · E3.4 `6668f81` · E3.6-E3.13 (encuadre) · B0.5 `76d2f1a` · **E3.7 (este)**. Cola después del eyeball: `ENCARGO-CODE-metrica-desde-CM-2026-07-17.md` v3 → luego E4.

<!-- ── HISTORIA (17-jul mediodía, YA CERRADO): re-gate E3.1 ── -->
PASO ANTERIOR (hecho — el re-gate E3.1 **PASÓ** con los 3 videos de Luciano; E3.2 cerró los hallazgos de SEGUNDA/TERCERA PRUEBA): el gate E3 inicial **NO pasó** (eyeball `VIDEOS PRUEBA/EYEBALL THE AKASHA.mov`): 2 bloqueantes + 1 menor, los 3 arreglados. **Abrí /dev → 🛰 Centro de Mando** y medí: **(0) ANTES de entrar a Akasha** — el Despacho tiene que mostrar los MISMOS datos reales que /exec (era el BUG A); **(1) pill «⟶ Akasha»** → el universo entero (estaciones + Espacios + Muelle + Núcleo) visible **en <5s**, con el HUD «Memoria: n/7» llenando la constelación después, sin frenar nada (era el BUG B); **(2) lado a lado contra `AKASHA-prototipo-E113.html`**; **(3) fps** desktop y **≥30 iPhone PWA**; **(4) 5 toggles** sin pantalla negra y el orbe del CM retoma; **(5) una aprobación REAL desde el Muelle** (ejecuta de verdad); **(6) Vista Plana**. La promoción a /exec la decidís SOLO vos.

**LOS 3 FIXES (E3.1, commit de esta tanda):**
- **BUG A (bloqueante) — el CM quedaba a medio bootear. CAUSA RAÍZ ENCONTRADA:** el CM localiza sus cards por el **TEXTO de su `.eyebrow`** (`ccCard()` → `#centro .card .eyebrow`, patrón "evita IDs nuevos en el markup") y el Atril de Akasha usa **los mismos 6 textos**. Como E3 metió `#akasha` DENTRO de `#centro` y **antes** del cockpit en el DOM, `ccCard()` devolvía la card de Akasha: **las 6 cards del CM se pintaban en paneles ocultos**. La topbar se salvaba porque va por `id`. **Fix: `#akasha` vive FUERA de `#centro`** — mata la clase entera de bug (cualquier selector `#centro …` del CM), no un síntoma. Verificado: `#centro` es `position:static` (no crea stacking context) ⇒ el fondo (`.galaxy/.photobg/.veil`, fixed + z-index negativo) se sigue viendo detrás, y las 57 vars CSS que usa Akasha resuelven por su propio bloque de tokens + `:root` (0 huérfanas).
- **BUG A (2) — REGLA NUEVA de Bastión, implementada:** los maniquíes del markup (números inventados + botones de apariencia **operativa**: "Aprobar plan", pulgares Sí/No) se **limpian en el boot ANTES del primer fetch** (`ccLimpiarManiquies()`, antes de `cmRestaurarSnapshot()` para que un snapshot real repinte encima). Si `datosHoy` falla → `ccAvisoFuente()`: card vacía con aviso discreto. **Ojo para el futuro:** `ccEstado`/`ccTareas`/`ccAgentes` hacen `if(!g)return` sobre su contenedor (`.sys-grid`/`.tsum`/`.ag-list`) — si los borrás, esas cards **no vuelven a pintar nunca**; por eso a esas se les limpia el CONTENIDO, no la card.
- **BUG B (bloqueante) — segunda ola.** `cargar()` = primera ola (6 llamadas baratas) → **se construye el universo entero YA**. `cargarCerebros()` = segunda ola: un `cerebroGrafo` por Espacio, **timeout 12s por cliente**, cada respuesta agrega sus nodos y repinta (`refrescarNucleo()`); el que falla queda "sin constelación" y **no frena al resto**. HUD `#ak-cerebros` («Memoria: n/7») + el panel del Núcleo dice la verdad mientras carga. Guardia de generación (`AK.gen`): si salís y volvés, las respuestas viejas se descartan. **Medido en el harness: universo construido en 28ms con `cerebroGrafo` a 3000ms × Espacio** (antes: esperaba los 7).
- **BUG C (menor) — era PRE-EXISTENTE, no lo trajo E3** (está igual en el backup pre-E3): tras el cierre del `<style>` principal quedaban 2 reglas SUELTAS + un cierre de más ⇒ el parser cerraba el `<head>`, las tiraba como nodo de texto al `<body>` y **no se aplicaban**: por eso el starfield viejo del CM (`#cmSpace`) nunca se ocultaba. Movidas adentro del bloque. **Trampa que me comí y anoté en el código: dentro de un bloque de estilo, la secuencia de cierre literal termina el elemento AUNQUE esté dentro de un comentario CSS** — el primer intento de fix reintrodujo el bug al describirlo en un comentario.

**VERIFICADO OFFLINE (E3.1):** parser HTML: 0 cierres sin apertura · 0 sin cerrar · `<style>` 2/2 · primer nodo de texto del body = `☰` (ya no CSS suelto) ✓ · 0 ids duplicados ✓ · cada `ccCard()` matchea **exactamente 1** card del CM ✓ · `#akasha` fuera de `#centro` ✓ · 0 ids que Akasha pida y no existan ✓ · `node --check` del script inline ✓ · **harness verde**: adaptador OK, universo en 28ms sin esperar la memoria, Núcleo nace vacío (0) y llega a 11 nodos por la segunda ola, 5 toggles, dispose+forceContextLoss 6/6, `AK.on=false` al salir, 0 console.error ✓ · con `LENTO_MS=14000` (todos los cerebros por encima del timeout): la Oficina queda entera y el Núcleo sin constelación, 0 console.error ✓ · guardia diff repo↔GAS: solo difiere `index.html` ✓. **Lo que el harness NO puede decir: fps y render reales** — eso es tu gate.

**Decisiones de producto (Luciano, 17-jul):**
- **Núcleo = fail-closed sin etiquetas (opción 1).** `cerebroGrafo()` devuelve `{dim, alert}` **sin texto, a propósito** (Bastión: "Nunca etiquetas/atributos"). El panel de nodo muestra **dato real** — dimensión · cobertura (`alert` = punto ciego <40) · Espacio — y **nada inventado**. La constelación **agrega los Espacios** (cada nodo etiquetado con el suyo; `cliente.reg` = sus índices). Descartado: etiquetas en bulk y Núcleo sin panel.
- **4º estado `fail`** en las estaciones: un agente con la última corrida en error **no se ve "En reposo"** (chip rojo "Con error").
- **Botón «☄ Demo» fuera.** Simulaba salud 82%: un simulador de estado en prod. El clima lo maneja `estadoSalud()` real (<90 → ámbar). El demo cambia de casa: el prototipo standalone es la pieza de demo; KAIROS/reels lo cubre **«Sato co-presenta» (E4)** sobre datos reales.

**E3.5 (encolado — post-gate, antes o junto al encargo métrica):** **`cerebroNodo(idCliente, indice)`** — fetch **on-demand** de etiqueta/tipo/cobertura de **UN** nodo al clickearlo. La etiqueta ya existe en la hoja `nodos` del cliente (la escribe `upsertNodo`). Postura de Bastión que lo habilita: se veta **exportar el conocimiento entero al DOM en bulk**, no mostrarle **un** dato a Luciano autenticado al tocar una luz — misma superficie de confianza que las aprobaciones y ventas que el CM ya muestra. **Bulk anónimo, detalle bajo demanda.**

**Cabos sueltos conocidos (no bloquean el gate):** (a) **avatares reales** llegan al Atril/paneles DOM pero **los badges 3D siguen con el glifo** del prototipo (la spec visual) — como textura es E3.5/E4; (b) `#hud-bottom-right` es una regla CSS sin elemento (huérfana ya en el prototipo); (c) `pa-m3` es **"ok hoy"**, no "avisos": no hay fuente que impute avisos por agente; (d) **`_akasha_e3/splice.py` NO se puede volver a correr** — reconstruye desde el backup pre-E3 y borraría los fixes de E3.1; `index.html` es la fuente de verdad (avisado en su README). Lo vivo ahí es `harness.js`.

<!-- PASO ANTERIOR (17-jul mañana, YA HECHO — commits a8b95ff/87347ef): AKASHA E2 spike + P0.5. GATE E2 PASADO (Luciano, 17-jul: 100 fps desktop · r128 · toggle OK). El spike era andamio: E3 lo reemplazó por el motor del prototipo. El paso 17-jul previo (gate @28 en prod) quedó CERRADO y verificado. -->
<!-- PASO ANTERIOR (16-jul tarde, YA HECHO — commit 5e4a097): _ui_avatares_code.sh -->
PASO ANTERIOR (hecho): **`bash _ui_avatares_code.sh`** = 16-jul tarde: **(1) glow eclipse del aro restaurado** — `.oring.a` recupera el stack de resplandor del /exec (5 capas: dorado 30px → terracota 70/130px → jade 200px + inset cálido) sobre el trazo fino de 1px a `.86` (la distancia nueva queda; solo vuelve el "resplandece eclipsado"); estado listen re-escalado. **(2) Avatares propios Bandeja y Cerebro** — los 2 nodos nav ganan `data-ag="bandeja"/"cerebro"`, `cmAvataresOrbita(agentes,cfg)` ahora también mapea `est.cfg.avatar_bandeja/avatar_cerebro` (fail-closed al glifo si la key no existe) y `estadoAgentes` expone las 2 keys nuevas de Config. **REQUIERE PASO MANUAL:** subir `Avatares Satori/Bandeja.png` y `Cerebro.png` a Drive y sembrar Config `avatar_bandeja`/`avatar_cerebro` con `https://drive.google.com/thumbnail?id=FILE_ID&sz=w512` (los 3 intentos de subida por MCP fallaron: 2 por permission-stream, 2 por base64 a mano — canal descartado). Verificado: node --check (index + 08_webapp), render local con glow OK. Después: eyeball /dev → `_promote_exec.sh --go` → **@26** → `kickstart -k` agente voz → prueba "cuántas órdenes tuvo Vehemence en julio". Sigue: **lanzar a Code `ENCARGO-CODE-F2-contrato-status-report.md` + `ENCARGO-CODE-mantenimiento-cola-2026-07-16.md`** · remoto GitHub OV (pasos entregados) · guion EJF WhatsApp (entregado).

**T1+T2 YA EN GAS+GitHub (commit `034f3d5`, 08-jul 10:39):** `_trillion_t12_code.sh --go` corrió limpio · `setup()` OK · `selfTest()` = **TODO OK con los 7 D9** (`APR-0005` creada por D9 B2 en prod) · **botón "→ Crear aprobación" VERIFICADO EN VIVO** vía demo (rec anclada a CLI-001 → botón → creó APR-0002 P1 en FRANFLACA). Falta solo eyeball escroleando `/dev` (Actividad/Calendario/board F1) para el cierre @20.

## Contexto del día (07-jul, consolidado)

**Mañana/mediodía:** promoción `/exec` @17 (kill-switch #7 voz) · voz revivida (watchdog+KeepAlive, STT es-419) · P2 F1-F3 verificadas en /dev (video 13:27) · P2 F4+F5 → **@18** (`selfTest` TODO OK) · B7 marco de cartera entregado y corregido (`consultoría/B7-marco-cartera-2026-07-07.md`). **Tarde:** norte v9 cerrado contra CM vivo (estados de voz `setSato` commit `c7c7077` + Agenda opción A commit `f12abb0`) · importador Trello (23 tareas) corrido → **DEPLOY @19 EN PROD ✅ confirmado por Luciano** (Agenda + Tareas + lazo F1-F5 + contrato brief + estados voz). **Sesión de cierre (esta, tarde-noche):** ver Verificado.

## Estado vigente
Satori OS = ERP multi-tenant **GAS + Sheets** en prod (`luciano@satoriconsultoria.com`) + Voz (CM desktop + PWA iPhone por Tailscale). **SISTEMA OS TERMINADO: P2 completo (lazo F1-F5) + Agenda + 23 tareas Trello en prod `@19`, confirmado por Luciano.** B1-B5 ✅ · P2 F1-F5 ✅ · norte v9 ✅. Resta SOLO el paso Code de esta sesión de cierre (ver PRÓXIMO PASO). Después el programa es **NEGOCIO — B7** (SIP al regreso de Nicolás · Vehemence monto ARS · build-in-public decisión) + **research Tareas-v2** (`docs/TRELLO-a-Satori-mapeo.md`: alta rápida desde board, recurrencia real, etiqueta contexto) + **B8 FINAL** (datos reales + Bucket B + RGPD + `puesta-en-marcha`) con gatillo de cliente real. Motor/método (fase→skill, F2/F3, brief-push) = diferidos con gatillo. Fuente de plan: `consultoría/PLAN-ACCION-INTEGRAL-SatoriOS-2026-07-06.md`.

### Verificado
- [16-jul noche · **Voz pulido post-@27: espejo de aprobaciones + N9 + N5 verificada** · `_voz_pulido_code.sh` → `--go` (clasp /dev + git)] **Contexto: `accion` FUNCIONÓ end-to-end en @27** (confirmación verbal → APR creada → mensaje N5 correcto), pero la aprobación no aparecía en el CM. **(1) BUG DEL ESPEJO — cerrado.** Diagnóstico verificado contra el código: `crearAprobacion` escribe en la hoja `Aprobaciones` del **CLIENTE**; el CM lee el espejo `Aprobaciones_agregadas` del **MAESTRO**, que solo reconstruía `syncMaestro` (1×/día) ⇒ la voz creaba y no se veía hasta la corrida siguiente. **Fix: `agregarAgregada_(cli, fila)`** — el inverso de `quitarAgregada_` (Delta 2, 08-jul), llamado desde `crearAprobacion` ⇒ **cubre a TODOS los callers** (voz, T2 B2, futuros). NO se llama a `syncMaestro` (abre todos los Sheets cliente, 15-30s: colgaría el doPost de voz — lección SPEC-GAS 14-jul). **Los 2 matices, resueltos leyendo el builder real, no asumiendo:** (a) **el espejo lleva SOLO `estado==='pendiente'`** (criterio exacto de syncMaestro) ⇒ una auto-aprobada por Dirección **NO entra**: no espera decisión de nadie; (b) **fail-safe**: si el espejo falla se loguea y se sigue — la aprobación del cliente ya está escrita y durable, y el sync de la mañana la repone. **IDEMPOTENCIA verificada:** `syncMaestro` es **wipe-then-rebuild** (`clearContent`+`setValues` desde los Sheets cliente) ⇒ la fila incremental **no se duplica**. **★ RIESGO que el encargo no anticipaba:** `crearAprobacion` YA corre dentro de `conLock`, y anidar otro lock haría que el `finally { releaseLock() }` del interno **soltara el lock del externo**, rompiendo la atomicidad de `nextId`+`append` (independiente de si LockService es reentrante) ⇒ **el espejo va FUERA del lock**, después del `flush`. Usa `appendFila` (mapea por header, sanitiza y respeta `COLUMNAS_TEXTO`: `'APR-0001'` no se coacciona), no `appendRow` crudo. **(2) REGLA N9** en `agent.py`: una tool que falla se dice, con los TRES caminos reales (reintentar · el CM · dejarlo anotado con `capturar`) y **jamás un canal inventado** ("si no está en tus herramientas, no existe"). Origen: en el log de las 16:23 Sato recomendó "comunicate con **Cloud Pro**" — canal inexistente, probable eco STT de "Claude Pro". **(3) N5 — VERIFICADA E INTACTA, sin cambios.** El log prueba que **Sato SÍ llamó a `capturar`** (16:23:43, texto "No se pudo registrar el objetivo para Vehemence… inconveniente interno", sin `no-ok` ni `ToolError`) y **recién a las 16:24:05** dijo "dejé registrado el motivo": afirmó la acción **después** de ejecutarla, que es exactamente lo que N5 exige. **No era afirmación-sin-acción; no se tocó el prompt.** **(4) Nombres gemelos — resuelto con rename:** `archivarColaVieja()` → **`archivarColaViejaREAL()`**. Motivo medido: por orden alfabético la **destructiva aparecía PRIMERO** en el desplegable, antes que `verifArchivoCola()` (el dry-run). Se descartó el `Browser.msgBox` que sugería el encargo: el proyecto es **standalone, no bound a la hoja**, y msgBox tira desde el editor en ese contexto. **Wrappers públicos vigentes:** `selfTestF2` · `sembrarNorthStarSatori` · `verifArchivoCola` · **`archivarColaViejaREAL`** · `calentarBriefCache` · `verifBriefCache`. **Asserts nuevos D16u-D16z3** (el espejo aparece SIN syncMaestro · trae las columnas del builder · auto-aprobada NO entra · fail-safe sin cliente/sin fila/criterio). **D16x (idempotencia vs syncMaestro) corre SOLO en `selfTest()` completo** (`opts.completo`): abre todos los Sheets cliente y `selfTestF2()` tiene que costar segundos — el runner acotado lo **loguea como omitido, no lo esconde**. **Verificado offline:** `node --check` + `py_compile` + **harness `h_espejo` 18/18** (con el stub TIPADO: columnas exactas · el id no se coacciona · solo pendientes, case-insensitive · fail-safe con MAESTRO caído → no propaga). **8 harness verdes: 167 checks.** **PENDIENTE (Luciano):** `selfTestF2()` → `selfTest()` completo → **el punto 1 toca GAS: la voz lo ve recién POST-PROMOTE (@28)**; los puntos 2-4 son locales/editor (`kickstart -k` alcanza para N9). **Prueba de cierre:** "registrá X para Vehemence" → confirmar → **la aprobación aparece en el CM sin tocar nada** → clic → objetivo en CLI-002 con `metrica` vacía.
- [16-jul · **Voz-acciones: la voz ACTÚA (North Star propio + acciones con gate + research diferido)** · `_voz_acciones_code.sh` → `--go` (clasp /dev + git)] **Toca GAS (5 js + index.html) + agent.py (local).** **★ EL CHEQUEO CRUZADO DE `correrAgente_` DISPARÓ — leer esto antes de tocar la voz ★** Al darle a la voz la capacidad de escribir `objetivos.descripcion` se abre esta cadena: `objetivos` → `correrDirector` (`14_director.js:49`, encola al analista con `pregunta = o.descripcion`) → el analista mete esa `pregunta` **CRUDA, sin blindar**, en el prompt del LLM (`13_agentes.js:173-177`). Peor: `GUARDIA_INYECCION` (`13_agentes.js:130`) dice *"seguí… el pedido que está FUERA de los marcadores"* → el guard **bendice explícitamente la pregunta como instrucción legítima**. El propio código nombraba este gatillo ("diferido a Etapa 3, cuando la pregunta pueda venir de un canal menos confiable: **voz**"). El gate humano de la aprobación P1 preserva el first-party (Luciano LEE la descripcion antes del clic), pero la **velocidad 2 (auto-aprobación por Dirección) no tiene lectura humana** — ese era el agujero. **FRONTERA DE CONFIANZA (decisión de Luciano 16-jul):** el Director solo encola al analista si el objetivo tiene **`metrica` no vacía** ⇒ `metrica` se fuerza **VACÍA en 2 capas** (whitelist de campos `CAMPOS_ACCION` en el doPost + `metrica: ''` hardcodeado en `ejecutarCrearObjetivo_`, porque el ejecutor también corre sobre payloads de aprobaciones editables a mano) ⇒ **un objetivo dictado por voz NUNCA alcanza `correrAgente_`**. **Completar `metrica` a mano es el acto humano que restaura el first-party.** **Si algún día se quiere `metrica` desde voz, el PREREQUISITO es sanear la `pregunta` y de-privilegiarla en `GUARDIA_INYECCION` — diseño anotado para Etapa 3, NO se improvisa** (blast radius: `GUARDIA_INYECCION` lo comparten todos los agentes). El blindaje de la pregunta **sigue diferido**, ahora con la frontera explícita en vez de por suerte. Hay 2 guardias en el script de deploy que abortan si alguien mete `metrica` en cualquiera de las 2 capas. **P1 North Star propio:** `sembrarNorthStarSatori_()` idempotente. **FUENTE ÚNICA = Config** — **HALLAZGO: la premisa del encargo ("no está registrado en ningún lado") es falsa**: `northStarSatori_()`/`cargarNorthStarSatori()` ya existían, ya computan el avance (clientes pagos vs meta) y el contrato F2 ya lo renderiza; solo faltaba *poblarlo*. Sembrarlo además en `CLI-000/objetivos` (como pedía el encargo) habría creado 2 fuentes derivando → **decisión de Luciano: Config para el norte, `objetivos` de tenant para lo dictado** (son datos distintos, no dos casas del mismo). Guarda dura: `_hueleANorthStar_` rechaza crear el North Star de sistema por voz. Deadline **31/12/2026** = lo dictado en voz (el PAQUETE 10-jul sugería 31/10; cambiarlo = 1 celda `ns_satori_horizonte`). **P2 tool `accion`:** whitelist dura `ACCIONES_VOZ` (v1 = **solo `crear_objetivo`**, escalera 0→1) → **NO escribe directo**: crea Aprobación P1 tipada → el clic la materializa; con Dirección vigente (F2) auto-aprueba y ejecuta en el turno **citando el id real creado** (evidencia, no promesa). Bastión: tenant SOLO del roster (nunca un id del LLM), whitelist de campos server-side, `limpiarHostilTexto_`, cap 4KB, `conLock`. **P3:** `estadoAgentes.north_star` + card `cmNS` (oculta sin dato, fail-closed). **P4 research:** prefijo `[RESEARCH]` → bin `research` **sin gastar Haiku** (es determinista) + el brief los lista en "Espera tu decisión". La web NO se le da al agente (dictamen Bastión). **P5 STT:** `keyterm` ampliado al léxico real — **ya existía con 8 términos** (`agent.py:467`), verificado contra `livekit-plugins-deepgram` **1.6.4** instalado (soporta `keyterm`, no asumido; el script aborta si el plugin dejara de soportarlo) + regla **N8 anti-fantasma**. **Prompt:** N6 (norte de Satori vs objetivos de tenant — el gap 1 del log), N7 (research honesto), N8. **P6 — HALLAZGO de premisa: `CAPABILITIES.md` NO es "el catálogo que Sato consulta". Nadie lo lee en runtime** (grep: ni `agent.py` ni ningún .js lo abren) — es un doc para humanos/Cowork, autogenerado por introspección. Que Sato dijera "no encontré documentación sobre cómo registrar objetivos" NO se arregla regenerándolo: el conocimiento de Sato = system prompt + docstrings de las tools. **El arreglo real de ese gap es el docstring de la tool `accion`** (dice qué registra, que deja una aprobación, y que el North Star no va por ahí) **+ N6/N7**, que sí están. CAPABILITIES se regeneró igual (higiene: el hook pre-push lo exige) y ahora lista `accionVoz_`/`esResearch_`/`sembrarNorthStarSatori_`, pero eso es introspección de nombres, no un how-to. **Si se quiere que Sato consulte de verdad un catálogo, hace falta una tool que lo lea — no existe y no se inventó acá.** **Verificado offline:** `node --check` 5 js + script de `index.html` parseado + `py_compile` + guardia diff repo↔GAS + **harness node 25/25** (whitelist de acciones · tenant fuera del roster · payload no-objeto/gigante · **`metrica` colada en el payload → descartada** · saneo hostil · North Star por voz → rechazo sin falsos positivos · N5: sin Dirección no afirma que ya ocurrió). **Asserts D16 (a..t).** **BUG cazado por el harness:** mi test de N5 buscaba el substring "registrado" y daba falso fallo — el mensaje dice "un clic y **queda** registrado" (futuro condicional, la frase literal del encargo); el test ahora verifica que no haya afirmación en pasado y que `estado==='pendiente_aprobacion'` sea la fuente de verdad, no la prosa. **PENDIENTE (Luciano):** `setup()` → `selfTest()` (D16 TODO OK) → **`sembrarNorthStarSatori_()`** (sin esto la card del CM no aparece: es fail-closed) → eyeball `/dev` → `_promote_exec.sh --go` → **`kickstart -k`** → prueba de voz: (1) "¿cuál es mi north star?" → **el tuyo**, no el de Vehemence · (2) "registrá el objetivo X para Vehemence" → repregunta + aprobación en el CM · (3) "investigame X" → `[RESEARCH]` en la Bandeja · (4) un nombre inventado → repregunta. **OPCIONAL (decisión tuya):** sembrar en `Direcciones` una fila `crear_objetivo` + `alcance=CLI-000` + vigencia futura + `activa=si` → "registrá X" queda registrado en el mismo turno sin clic. **Recomendado NO sembrarla para tenants de clientes.**
- [16-jul · **Mantenimiento: dieta Cola_tareas + avisos agrupados + rename fila OV** · `_mant_cola_code.sh` → `--go` (clasp /dev + git)] **Toca GAS (4 js). NO toca la voz ni la UI.** **(1) Dieta (`12_cola.js`):** `archivarColaVieja_` mueve las filas TERMINALES con >30d a `Cola_archivo` bajo `conLock`; **escribe y flushea ANTES de borrar** (es destructivo-móvil: al revés, un corte entre borrar y escribir perdería filas), borrado batch bottom-up (`borrarFilasBatch_`), idempotente. Hook al final de `corridaDiaria` (tras el warm del brief), falla-silenciosa. **HALLAZGO que corrige el encargo:** el encargo mandaba archivar los estados `hecha`/`error`/`cancelada` — **esos no existen en la cola**: son de la hoja `Tareas` (el kanban). Los terminales reales de `Cola_tareas` son **`completada`/`fallida`** (`12_cola.js:81-82`); implementado literal, la función **no habría archivado nada**. (Lección del 08-jul aplicada: todo marcador literal se valida contra el archivo real.) **Las 2 protecciones obligatorias:** nunca se archiva la **última fila de cada agente** (`estadosAgentesCola_` deriva el estado del agente de su última fila → archivarla lo dejaría "idle" sin serlo) · nunca se archiva una fila del **mes en curso**. La decisión se extrajo a **`_colaArchivable_` (PURA)** justamente para poder probar cada guarda sin depender del día en que corra el test. **(2) Conteo de errores (`08_webapp.js`, decisión de Luciano 16-jul):** `telemetriaMaestro_` contaba `estado==='fallida'` sobre la cola **entera, sin ventana**, pese a que su propio docstring decía "del mes" — **bug latente**: era un contador histórico disfrazado de mensual, y archivar se habría comido los errores viejos. Ahora cuenta **el mes en curso**, lo que arregla el bug y hace que archivar no pueda alterarlo (horizonte ≥ ventana, garantizado por la protección 2). **El histórico no se pierde: queda en `Cola_archivo`, consultable desde el editor — pero fuera del poll**, que es el punto del encargo (`08_webapp` NO lee `Cola_archivo`; hay una guardia en el script que aborta si alguien lo agrega). **OJO producto:** la tira "errores" del CM ahora baja el día 1 de cada mes — es el comportamiento que el código decía tener. **(3) Avisos (`06_avisos.js`):** con **>3** estancadas → **1 aviso resumen** citando las 3 más viejas **por fecha de creación**; con ≤3 siguen individuales; los individuales viejos se resuelven por baseline (`resolverAvisosDonde_`, marca `resuelto`, no borra: append-only). **(4) Rename (`08_webapp.js`):** `Negocio paralelo pausado` → **`Oficina Virtual — kill-switch (np_pausado)`** (el nombre viejo + valor `no` se leía como si la Oficina ESTUVIERA pausada → falso diagnóstico del 14-jul). **El encargo temía fila-vieja+nueva conviviendo: NO puede pasar** — el reemplazo idempotente del sync borra por **FUENTE** (`'Oficina Virtual · sync'`), no por concepto (`08_webapp.js:275`) → la vieja se va sola en el primer sync; no hace falta migración. Único "lector" del concepto viejo era el assert D12 (no hay lector en CM/brief) → actualizado. **Verificado offline:** `node --check` 5 js + guardia diff repo↔GAS (solo los 4 esperados) + **harness node 20/20** sobre `_colaArchivable_` (terminales reales vs los del encargo · horizonte exacto · sin fecha · las 2 protecciones · **el caso del día 31**: fila del mes en curso con >30d de edad → NO se archiva). **Asserts D15 (a..l) + D12b.** **PENDIENTE (Luciano):** `setup()` → `selfTest()` (D15 + D12/D12b) → **`verifArchivoCola_()` en el editor: DRY-RUN, dice cuántas archivaría hoy sin mover nada — mirá el número antes de dejar que corra la diaria** (la cola tiene ~857 filas) → `archivarColaVieja_()` a mano la 1ª vez y comparar (Cola_tareas más chica + Cola_archivo con las filas + **la tira de errores del CM IGUAL**) → eyeball `/dev` → `_promote_exec.sh --go`.
- [16-jul · **F2: Contrato de Status Report v1 + direcciones pre-aprobadas** · `_f2_contrato_code.sh` → `--go` (clasp /dev + git)] **Toca GAS (4 js). NO toca la voz ni el CM.** **Contrato (`18_direccion.js`):** `contratoStatusReport_` renderiza las **10 secciones en orden FIJO** (BLUF anclado → apertura humana/agenda → métricas vs North Star con tendencia → auto-resuelto + "aprendí y ya ajusté" → espera tu decisión → recomendación priorizada → cierre acción→métrica → insumos → señal de instrumentación → cierre + feedback). Decisión de diseño: **una sección sin dato NO se omite** — emite `(sin dato)`; el hueco silencioso haría creer que la sección no aplica. `_tendencia_` calcula la tendencia **real** entre los 2 últimos puntos de la serie y **devuelve null si no hay serie** (el NS de sistema no tiene histórico → el brief lo dice; inventarla sería el primer número falso del reporte). `_recContractual_` = dato ancla + **contrapeso de riesgo** + acción (EXTIENDE T2, no lo reemplaza: la rec sigue saliendo de `recomendacionDelDia_`). `_cierreAccionMetrica_` lee el lazo F1-F5 ya existente. Los dos briefs (sistema **y** cliente) se re-escribieron sobre el contrato **solo con lecturas baratas del MAESTRO** — el render frío de este brief es el que colgaba el doPost de voz (SPEC-GAS 14-jul), así que no se abre ningún Sheet cliente de más. **Direcciones (P2.8, `11_aprobaciones.js`):** hoja nueva + `direccionVigente_` con match **EXACTO** (tipo_accion Y tenant), **vigencia obligatoria**, **sin wildcard** (`*`/vacío no matchean), revocable por `activa=false`, todo **fail-closed**; `crearAprobacion` la consulta → la fila nace `aprobada` citando `direccion:DIR-nnn` en `decidido_por`+`notas` y deja rastro en Actividad. **Default-deny intacto para todo lo demás. AUTO-APROBAR NO AUTO-EJECUTA** (la ejecución sigue por `ejecutarAprobada()`; el encargo no pidió ejecución automática y no se la dio). **Verificado offline:** `node --check` 4 js + guardia diff repo↔GAS (solo los 4 esperados) + **harness node 36/36** (orden contractual · fallback honesto · tendencia acelerando/frenando/estable/null · rec con contrapeso · **13 casos Bastión de direcciones**: vencida/sin vigencia/activa=no/wildcard/otro tenant/otro tipo → todos NO matchean). **Asserts D14 (a..q)** en selfTest + limpieza extendida (`DIR-TEST-*`, Feedback `__TEST__*`, `Cola_archivo`). **BUG cazado por el harness:** el chequeo de orden por substring daba falso fallo — `'## Cierre'` matchea primero `'## Cierre acción→métrica'` (sección 7); los asserts ahora comparan la **línea exacta**. **PENDIENTE (Luciano):** `setup()` (reconcilia Direcciones + Cola_archivo) → `selfTest()` (D14 TODO OK) → `briefDiario()` y `briefDiario('CLI-002')` en el editor (leer el log: 10 secciones, tendencia real) → eyeball `/dev` → `_promote_exec.sh --go`. **OJO: `Direcciones` nace VACÍA ⇒ la auto-aprobación está INACTIVA** hasta que siembres una fila a mano (tipo_accion + alcance=CLI-xxx + vigencia futura + activa=si). **Cabo abierto (decisión 16-jul):** el blindaje de la `pregunta` del analista (`13_agentes.js:170-173`) **sigue diferido** — la decisión documentada manda sobre el "es MUST" del encargo, porque el gatillo NO disparó (la pregunta solo nace de la hoja `objetivos`, first-party; ninguna tool de voz alcanza `correrAgente_`). **Gatillo explícito: blindar cuando la pregunta pueda originarse en voz o en un cliente del tenant — chequear al ejecutar `ENCARGO-CODE-voz-acciones` si la tool `accion` abre camino a `correrAgente_`.** **Fuera de alcance (hallazgo):** el encargo pedía aplicar el contrato también al **Informe Mensual KAIROS**, que **no existe como código en este repo** (solo se lo menciona en `docs/`) → el contrato quedó como renderer reutilizable (`opts.completo`) para cuando ese informe exista.
- [14-jul · **SGIC: Sato accede a TODO el SGIC del cliente** · `_sgic_code.sh` → `--go` (clasp /dev + git)] **Toca GAS + agent.py (local).** **Capa 1** (`estadoVigenteCliente_`, `18_direccion.js`): "Operación reciente" ahora incluye la columna `notas` de Datos_operativos (ahí viven las órdenes/AOV mensuales del conector), saneada+truncada → fluyen a la voz SIN tool nueva. **Capa 2** — tool **`sgic`** read-only (`08_webapp.js`): whitelist DURA de hojas (`Datos_operativos, KPIs, objetivos, estado_actual, Aprobaciones, Excepciones, Umbrales, Reglas, Costos_API`; cerebro NO) + caso especial **`ventas`** (fuente viva, mapa conector HARDCODEADO CLI-002→`VEHEMENCE_DB_ID`) → `{ordenes, total, aov, por_canal, cobertura}` EXACTOS vía `agregarVentasPorMes_` (ahora expone `canal/ordenes/aov` numéricos, aditivo — no cambia schema). **Bastión:** cero escrituras · solo el sheet del roster (`abrirCliente`) · nunca un id del LLM · todo string por `limpiarHostilTexto_` · respuesta cap ~8KB · `voz-timing` la mide. **agent.py:** `sgic_consulta(idCliente, hoja, mes)` con filler T1 + regla de prompt (usar antes de decir "no tengo el dato"; N4). **Verificado offline:** `node --check` 4 js + `py_compile` agent.py, guardia diff repo↔GAS (solo los 4 esperados), harness node **17/17** (ventas 2/300000/150000 exactos · por_canal · cobertura · saneo hostil sin \n\t+truncado · `_sgicMesDe_` multi-columna · whitelist · cap 8KB). **selfTest D13** (a whitelist→rechazo sin leer · b lee Datos_operativos __TEST__ · c mes filtra · d limite · e ventas fixture exactas · f celda hostil saneada) + **bonus** (resuelve por baseline el aviso `voz_acceso_no_autorizado` de prueba; `limpiarTodoTest` ya barre `tarea_vencida [TAR-TEST-1]` y `aprobacion_expirada [APR-TEST-EXP]` por marcador). **PENDIENTE (Luciano):** `selfTest()` en editor (D13 TODO OK) → eyeball /dev → `_promote_exec.sh --go` → **POST-PROMOTE** reiniciar agente (`kickstart -k`) + prueba de voz real ("cuántas órdenes tuvo Vehemence en julio" → número exacto de la DB viva). sgic funciona end-to-end recién tras el promote (el agente pega a /exec).
- [14-jul · **SPEC-GAS brief-lento + dieta del feed + polling** · `_voz_perf_code.sh`, deploy vía `--go` (clasp /dev + git)] **Toca GAS (OK dado).** 4 cambios: **(1) instrumentación** del doPost de voz (`console.log voz-timing tool/ms`) para medir server-time real. **(2) cache del brief** (`briefCacheado_`, CacheService): la ruta de voz `case 'brief'` lee cache (get endurecido try/catch, `if(hit)`), TTL **600s**; el render caro (estadoSalud 6 chequeos + Tareas) dejaba colgar el doPost 24-31s. **`corridaDiaria` calienta** el brief de SISTEMA al cierre (`calentarBriefCacheSistema_`, TTL **6h**) → la consulta de la mañana es HIT instantáneo (mata el render frío). `verifBriefCache_` (editor) mide render(miss) vs cache(hit). Read-only ⇒ solo TTL, sin invalidación. **(3) dieta del feed:** `feedReciente_` ya leía solo las últimas N filas (desde 12-jun, `4b27343b`); se agrega **truncado del texto a 240** (celdas de Actividad gigantes). **(4) polling CM 5s→15s** (index.html:1031) → baja la tormenta que contendía con el doPost de voz. **Verificado offline:** `node --check` 3 js + index, guardia diff repo↔GAS (solo difieren los 4 esperados, clasp OK), harness node cache 7/7 (miss computa · hit no recomputa · TTL 600/warm 21600 · get-throw degrada · warm→HIT) + truncación 5/5. **HALLAZGO que corrige el encargo:** las "857 filas enteras" NO son Actividad (feedReciente_ ya acotado) sino **`leerTabla(Cola_tareas)` sin poda** en `estadoAgentes` → dejado como **propuesta diseñada** en la SPEC (Opción A archivar / B agregados; riesgos: conteo de errores + último-estado-por-agente) para encargo aparte con OK. **PENDIENTE (Luciano):** eyeball `/dev` + `_promote_exec.sh --go` (prod /exec: voz doPost + CM toman el cambio recién ahí) + `verifBriefCache_()` en editor.
- [14-jul · **ENCARGO voz-colgada: timeout duro + fallback hablable** · commit `3c7e818` pusheado a `main`] **Solo `voz/agent/agent.py` (local, sin GAS/clasp).** **Diagnóstico con logs reales (`~/Library/Logs/satori-voz-agent.log`, job `AJ_ffLarVEYuxBW` 08:22-08:24):** el `brief` GAS colgó los **30s enteros** (`Read timed out (read timeout=30)` contra `script.google.com`); a los ~18s Luciano habló, LiveKit **canceló la speech** pero el thread HTTP bloqueado no se puede matar y disparó un `brief` nuevo → se encadenaron **4 `brief`**, cada uno colgado ~25-30s, y el `ToolError` caía en un turno YA cancelado → **jamás se habló**. Ninguna excepción tragada; el HTTP esperó de verdad. **Fix (cliente):** `_llamar_backend` gana tope TOTAL wall-clock `asyncio.wait_for(25s)` → al vencer devuelve `_MSG_BACKEND_TIMEOUT` (hablable), fail-closed, **sin reintento** (brief no idempotente); el turno SIEMPRE termina con respuesta. **Downstream break blindado:** `capturar` (escritura) comparaba nada → ahora si el backend hace timeout NO dice "Anotado" (no confirma guardado incierto, N5). **Prompt:** línea anti-promesa-vacía (no "lo sigo mirando"; ofrecer reintentar). **Verificado:** `py_compile` OK + harness headless `voz/agent/test_timeout_backend.py` **8/8** (sentinela hablable, corte real a 25s en el mismo turno, capturar no falso-confirma, camino feliz intacto) + agente **reiniciado** (`kickstart -k`, pid 42813, worker registrado sin traceback). **NO tocado:** VAD/STT/TTS/pipeline, tools Oficina, GAS. **Causa RAÍZ = server-side** (GAS tarda ≥30s; `briefDiario` es lectura pura, NO usa `conLock` → el lock NO lo bloquea, hipótesis del pedido #4 descartada por código) → **spec aparte, sin tocar GAS: `SPEC-GAS-brief-lento-doPost-2026-07-14.md`** (instrumentar doPost + cache corto del brief). **PENDIENTE supervisado (Luciano): prueba de voz real** ("el brief del día" con backend lento) — conductual, no unit-testeable.
- [14-jul · **E2+E3: la Oficina se gestiona DESDE Satori OS** · commits `6cddcdc` (E2 voz) + `65d9a1f` (E3 GAS), pusheados + clasp push /dev] **FASE 2 code-completa (salvo pasos de prod de Luciano).** **E2.1 kill-switch `np_pausado` server-side (OV, sin commit):** tabla config `np_pausado` (default false) + `config.set_valor`; `app.py` do_POST → **423** en decidir/accion si pausado (ruta del toggle `/api/v1/np_pausado` EXENTA para poder despausar), GET normales, expuesto en `/estado`; toggle solo desde el panel (botón `btn-np`, la voz nunca togglea). **Verificado end-to-end por curl:** activo→gate pasa · pausar→423 en decidir Y accion · GET→200 · reanudar exento→200. **E2.2 tool `oficina_decidir` (agent.py):** aprobar/rechazar vía POST `/api/v1/aprobaciones/decidir`; `_oficina_http` gana modo `devolver_status` (distingue 423 de caída; HTTPError=respuesta real sin retry; GET siguen fail-closed). **Verificado con DB real:** dummy → tool "quedá aprobada" Y `estado='aprobado'` en SQLite (escritura real, no alucinada) · pausado → 423 Y la dummy sigue `pendiente` · dummies limpiadas, id=4 real intacta. **N5 v2** reescrita (decidir con confirmación explícita obligatoria; el resto prohibido; solo afirma con resultado real). **E3.1 doPost `oficina_sync` (D1):** secreto DEDICADO `OFICINA_SYNC_SECRET` (whitelist por caller, ruteado antes de `vozAuth_`); payload v:1; títulos re-sanitizados server-side (`limpiarHostilTexto_`, defensa en profundidad + `sanitizarCelda` de appendFila); escribe CLI-000 Datos_operativos + KPI autonomía bajo `conLock`, reemplazo idempotente por fuente. **E3.2 tenant CLI-000:** `crearCliente` gana `forceId`; `asegurarTenantOficina_` idempotente. **E3.3 push (OV `observatorio/sync_cm.py`, sin commit):** OAuth Bearer (creds de luciano@, single source = .env de la voz) + 302-follow stdlib + secreto en body; hook al final de `ciclo-diario` + `ov.py sync-cm` manual; **FAIL-OPEN (D2) verificado** (push 404 → ciclo termina exit 0). **E3.5 D12** en selfTest (guard de versión, whitelist por caller, escritura CLI-000, refleja np_pausado, re-sanitización). `py_compile`/`node --check`/suite OV 43/43 OK. **HALLAZGO:** el doPost es **DOMAIN-access** (`executeAs USER_DEPLOYING`) → el sync necesita OAuth (no solo secreto); **`/dev` NO acepta Bearer (404 verificado)** → el sync real corre contra **/exec post-promote**. Transporte OAuth+302 verificado contra la /exec conocida (JSON limpio). Secreto: cero en repo/commits (grep), solo en `.env` (600) + Script Properties (paso Luciano).
- [13-jul · **E1.2 voz: fin del trabado-mudo + frescura + sync 8h** · commit `d78aedb`, pusheado + clasp push /dev] **T1 (el fix real del "anuncia y queda mudo"):** el anuncio hablado ahora se emite DENTRO de las 5 tools GAS lentas (`estado`/`brief`/`vehemence`/`cliente`/`cerebro`) vía `context.session.say(..., add_to_chat_ctx=False)` — API verificada leyendo livekit-agents **1.6.4** instalado (`RunContext.session` property + `AgentSession.say` síncrono). Fire-and-forget, atado a la ejecución → **imposible anunciar sin llamar** (el trabado era orquestación, no prompt; A5 no alcanzaba). Rotación de 3 frases; el filler nunca bloquea (try/except). Prompt: se ELIMINÓ la instrucción de anunciar-antes + A5, reemplazadas por "no anuncies, la herramienta lo emite". `oficina_*` (rápidas, <1s) y `capturar` SIN anuncio. **T2:** regla de prompt — los números de clientes son del último cierre sincronizado, no live; si implica el momento actual aclara "al último cierre de esta mañana"; la hora no la inventa (N4). **T3:** trigger nuevo `sincronizarConectores` `everyHours(8)` en `instalarTriggers()` (reset de `deseados` = anti-dup) — SOLO el sync de conectores; brief/avisos siguen 1×/día. `sincronizarConectores` es seguro suelto (solo `conLock`, sin emails — verificado). **Verificado:** `py_compile` OK, `node --check` 06_avisos OK, no-regresión (las 5 tools devuelven data con `context=None` → filler no-op; `oficina_estado` responde sin anuncio), guardia GAS↔repo (solo difería 06_avisos). Agente de voz reiniciado (PID nuevo, código E1.2). **NO tocado:** VAD/STT/TTS/pipeline, `_llamar_backend`. **Decisión:** T2-opcional (anexar timestamp real a `estadoVigenteCliente_`) OMITIDA — `ultima_sync_ok` es de `syncMaestro`, no del sync de conectores; con T3 (conectores cada 8h sin syncMaestro) citaría hora equivocada. El prompt cumple.
- [13-jul · **Flancos post-E1.1** (Cowork, OKs de Luciano) · commit `92f217a`] **N3 (CM):** la card "Coste del mes" usaba `toFixed(0)` → mostraba `$0/25` mientras el header mostraba `$0.27/25`; misma fuente (`c.gasto`), solo diferían por redondeo → card a `toFixed(2)`. **En /dev, pendiente eyeball+promote.** **N5:** `_promote_exec.sh` hardcodeaba el label del deploy ("Rediseño CM+Voz… 06-jul") para cualquier promoción → ahora usa el subject del último commit saneado (override `DEPLOY_DESC`). **F4/V1 — discrepancia ventas Vehemence (voz 15.799.927 vs live 16.498.963/150 órdenes): DIAGNÓSTICO, sin tocar pipeline.** La tool `vehemence` del doPost → `estadoVigente('CLI-002')` lee **`Datos_operativos`** (KPIs vacía); julio online 15.799.927 (bucket 07-01). Esa agregación la escribe `sincronizarVehemence()` desde `DB_VENTAS` (TiendaNube) **1×/día a las 07:00** (dentro de `corridaDiaria`, `everyDays(1).atHour(7)`; sin sync intradiario). `DB_VENTAS`/`VEHEMENCE_DB_ID` se leen SOLO en ese sync → el CM de Satori tampoco lee live: mostraría lo mismo. El 16.5M/150 órdenes = la **fuente viva** (DB_VENTAS/dashboard Vehemence), 699.036 por delante del snapshot de hoy. **Causa: cadencia, no fuente/cache.** Opciones para Luciano (todas tocan el pipeline → su decisión): subir cadencia del sync · voz aclara "al último cierre 07:00" · lectura live de DB_VENTAS. **Flancos OV (repo oficina-virtual, SIN commit):** ciclo diario TCC-fix + D4 tailnet + avatares del panel — ver `oficina-virtual/HANDOFF.md` y el handoff Fase 2.
- [13-jul · **E1.1 hotfixes de voz + UI CM** · 2 commits SatoriOS] **BLOQUE A (voz, `voz/agent/agent.py`) — verificado headless en venv:** A1 números hablados agrupados (prompt) · **A2 REGLA N5 anti-alucinación de ACCIÓN** (el hallazgo GRAVE del stress test 12/07: Sato dijo "aprobación realizada" sin tener tools de acción; default-deny aguantó, id=4 sigue pendiente — verificado por curl) — tools Oficina son SOLO-LECTURA, prohibido narrar acciones inexistentes, da el camino real (panel Observatorio) · **A3 conteos deterministas** en formatters con `len()` (test contra API viva: `Agentes: 4`, `pendientes: 1`, `20 hallazgos: 10 digitales, 3 físicas, 7 tendencias` — exactos) + prompt "conteos = N4" · A4 eco de captura ('Anotado: …', tool devuelve el texto exacto) · A5 anunciar-sin-llamar · **A6.1 retry** en `_oficina_http` (GET idempotente, 1 reintento sleep 0.3 — test con puerto muerto: 2 intentos logueados, apagada limpia, 0.31s <7s) · **A6.2** Observatorio YA es `ThreadingHTTPServer` (sin diff; intermitencia = carga/timeout, la cubre A6.1) · **A6.3 LaunchAgent `com.satori.oficina.observatorio`** cargado (KeepAlive verificado: kill→revive; usa `/opt/homebrew/bin/python3.12` porque `/usr/bin/python3` de CLT no tiene TCC sobre ~/Documents). Armonización (Cowork): la línea "alto nivel/números redondeados" que contradecía N4/A1 → "alto nivel = estructura, montos EXACTOS agrupados". **NO tocado:** VAD/STT/TTS/pipeline, `_llamar_backend`, tools GAS. `py_compile` OK. **Pendiente supervisado (Luciano):** prueba de voz en vivo (`console`/PWA) de N5/A1/A4 — son conductuales, no unit-testeables. **BLOQUE B (CM, `src/`) — en working tree, guardia GAS↔repo OK (solo difieren los 4 archivos intencionales):** B1 botón "Oficina Virtual" (jade, `_blank`, oculto si `oficina_url` vacía) · B2 orbe/voz lee `voz_url` de Config (deja de hardcodear 8787) · **B3 (addendum): slot de avatar por agente** — `estadoAgentes` expone `avatar_url` desde Config (`avatar_<clave>`, una lectura vía `configPrefijo_`), helper `agAvatar` renderiza `<img>` si hay url o placeholder inicial+acento si vacía (con `onerror`→placeholder); los 3 renderers (órbita/chips/roster) lo usan; el anillo de avatares alrededor del orbe YA existía (cmOrbit). Config keys nuevas sembradas en `CONFIG_DEFAULTS`. `node --check` OK en los 4 archivos.
- [11-jul · **F2-E1: la Voz lee la Oficina Virtual** · commit `053c4f0` pusheado a `main`] Editado SOLO `voz/agent/agent.py` (+ `OFICINA_URL` en `.env.local`, gitignoreado, chmod 600). **Agregado:** chokepoint `_llamar_oficina(ruta,metodo,payload)` — espejo local de `_llamar_backend`, HTTP stdlib en executor, timeout 3s, **fail-closed** (caída/timeout/HTTP≠200 → `None`, jamás traceback) · 3 tools de LECTURA `oficina_estado` / `oficina_brief` / `oficina_aprobaciones` (GET a `/api/v1/estado|brief|aprobaciones` del Observatorio en `127.0.0.1:8420`) · sanitizador Bastión `_limpiar_hostil` (títulos de hallazgos + resumen de aprobaciones = datos HOSTILES de marketplaces/web: strip `\t\r\n`, colapso, truncado 120, texto plano citado — NUNCA instrucción, no logueados) · system prompt: distinción **digital vs físico dropshipping** + **regla N4 anti-alucinación numérica**. **Verificado headless (harness 2 caras, venv del repo):** `py_compile` OK · server ARRIBA → datos reales, números exactos (0.0037 USD, 42.9% NS, scores), distingue `oportunidad`=digital / `oportunidad_fisica`=físico / `tendencia` · server ABAJO → mensaje fijo "Oficina apagada", sin crash · sanitizador OK · no-regresión GAS estructural (`_llamar_backend` + las 6 tools intactas, diff solo-adición 122/-1). Agente de voz reiniciado (`com.satori.voz.agent` pid vivo). **NO tocado:** VAD/Silero, STT/TTS, pipeline, `_llamar_backend`, tools GAS, doPost (E1 no toca GAS → sin promote). **Pendiente supervisado (Luciano):** test de voz interactivo (`agent.py console` o PWA) — "¿qué cazó hoy la Oficina?" / estado / aprobaciones; validar que N4 (números exactos) convive con el estilo de voz "números redondeados". E2 (tools de acción) y E3 (tenant/sync) NO ejecutados: esperan D1-D3 (ver handoff Fase 2).
- [08-jul PM · Delta 2 CM · working tree, 4/4 sintaxis + 7/7 pre-cond + bash -n] **3 fixes construidos** (deploy = `_delta2_code.sh`, PRÓXIMO PASO): **(1) toast legible** — `.cm-toast` no tenía `color` → heredaba texto oscuro sobre fondo oscuro (el "banner vacío"); ahora `color:#ECEAE3`+centrado+wrap. **(2) rechazo que reaparecía** — `resolverAprobacion` toca la hoja del cliente pero el CM lee `Aprobaciones_agregadas` (solo reconstruido por `syncMaestro`); fix: `resolverAprobacionUI` llama `quitarAgregada_(id)` (borra por id bajo `conLock`, bottom-up) → reflejo inmediato. **(3) A3 anclaje a KPI de cliente** — `recomendacionDelDia_` gana una rama (tras vencidas, antes de aprobaciones): si un cliente tiene un KPI con `alerta`, la rec se ancla a ese cliente (`clienteKpiEnAlerta_` escanea las hojas KPIs; solo corre en corrida/brief ~1-2×/día, NO en cada carga del CM; `pre.kpiAlerta` inyectable) → el botón cobra sentido en uso real. Asserts **D10** (A3) + **D11** (quitarAgregada_); D6 inyecta `kpiAlerta:null` para no escanear. Purga proporcionada: sin bloqueantes; el orden de prioridad de A3 (tras vencidas) es decisión de producto revisable.
- [16-jul · **FIX crash D16k + `chk` ACUMULATIVO + wrappers públicos** · `_runner_fix_code.sh` → `--go`] **Reportado por Luciano: crash en `_asertsF2_:686`.** **La defensa FUNCIONÓ y el test no la contemplaba:** D16k mandaba un título de ~5000 chars, `accionVoz_` capea el payload a 4KB → devolvió `{ok:false, error:'payload_grande'}`, el test siguió con `res.id_aprobacion === undefined` → `resolverAprobacion(cli, undefined)` → excepción. **LECCIÓN (4ª y última del día): un test que ejercita una superficie CON DEFENSAS debe esperar que las defensas SALTEN — el retorno de RECHAZO es un resultado esperado más, no un camino imposible.** **Arreglos:** D16k usa un título **por debajo del cap (~1500 chars)** para ejercitar lo que quiere probar (saneo `\n\t` + truncado a 200) y el cap se prueba **aparte en D16k2/k3** (>4KB → `payload_grande`, sin crash, y **nada escrito**). **Helper `_aprobarSiOk_(chk, cli, res, etiqueta)`:** jamás se llama a `resolverAprobacion` sin verificar `res.ok` — si la defensa saltó, se assertea el error (con el retorno real en el mensaje, para diagnóstico) y se corta esa rama. **Barrida con la lente nueva: 3 llamadas ciegas** (`D16g`, `D16j`, `D16k`) — con el `chk` fatal solo explotaba la última; **con `chk` acumulativo las 3 eran crash garantizado**. Todas pasan por el helper; `creado = creado || {}` evita que un D16g rojo se lleve puestos D16h/D16i con un TypeError. **★ `chk` ACUMULATIVO — el fix que corta el goteo de verdad ★** `selfTestF2_` registra y SIGUE; al final tira **UN** error con la lista completa (el log ya quedó impreso: no se pierde nada). **Una corrida = TODOS los rojos**, en vez de uno por vuelta. Las tandas van **AISLADAS**: `_asertsF2_` corre `_asertsD14_`/`_asertsD15_`/`_asertsD16_` cada una en su try/catch → una que reviente (excepción, o un undefined encadenado tras un rojo) **no se lleva puestas las otras dos**; la limpieza sigue SIEMPRE en el `finally`. **`selfTest()` completo mantiene su `chk` FATAL a propósito** (decisión, no olvido): sus fases legacy no están aisladas y un `chk` acumulativo ahí convertiría un rojo limpio en un TypeError crudo aguas abajo — **peor diagnóstico**. Es certificación: corta al primer rojo. El catch por tanda re-lanza con el chk fatal, así que el comportamiento de `selfTest()` no cambió. **★ WRAPPERS PÚBLICOS — el desplegable del editor NO lista las funciones con guión bajo final** (lección del 14-jul con `sgicConsulta_`, ya estaba en este HANDOFF y volví a pisarla). Luciano lo detectó en `selfTestF2_`, pero **la misma falla afectaba a las OTRAS DOS funciones que le pedí correr hoy**: agregados `selfTestF2()`, **`sembrarNorthStarSatori()`** y **`verifArchivoCola()`**. Las tres eran invisibles. **Verificado offline:** `node --check` 3 js · **harness nuevo `h_runner` 14/14** que prueba lo ESTRUCTURAL (aislamiento: D15 revienta → D14 y D16 corren igual y el fallo queda registrado · acumulación: 3 rojos en UNA corrida · el chk fatal re-lanza · `_aprobarSiOk_` con rechazo/undefined/ok · el wrapper público existe) — el aislamiento está **probado, no afirmado**. `h_voz` **29/29** (+4: cap >4KB → rechazo sin crear aprobación · ~1500 chars sí crea y llega saneado). **7 harness verdes: 149 checks.** Extracción de tandas verificada: **183 `chk` antes y después**. **PENDIENTE (Luciano):** correr **`selfTestF2()`** (SIN guión bajo, ahora sí aparece en el desplegable) → si TODO OK, **UNA** corrida de `selfTest()` completo → **`sembrarNorthStarSatori()`** → eyeball `/dev` → `_promote_exec.sh --go`.
- [16-jul · **FIX D16h (tipado de Sheets) + auditoría de TODA la batería + `selfTestF2_()`** · `_asserts_fix_code.sh` → `--go`] **Reportado por Luciano: `selfTest` falló en D16h** (`String(creado.fecha_objetivo) === '2026-12-31'`). **Causa: round-trip de Sheets.** `fecha_objetivo` NO está en `COLUMNAS_TEXTO` (que fuerza formato `'@'`) ⇒ Sheets **tipa la celda como Date** y devuelve un Date, no el string que se escribió. **★ LA TRILOGÍA DEL DÍA — todo assert se verifica contra las TRES realidades ★** (1) **el archivo real** (08-jul: `grep -qi`, marcadores literales) · (2) **los datos vivos** (D15k: `selfTest` corre sobre PRODUCCIÓN, nada de conteos hardcodeados) · (3) **los tipos que devuelve Sheets** (D16h: normalizar SIEMPRE — fechas con `aFechaISO()`, números con `Number()`, booleans con el patrón de `_dirActiva_`). **Un harness con stubs planos NO prueba la tercera** y por eso D16h se escapó: los stubs devolvían el eco de lo escrito. **Auditoría COMPLETA de la batería (180 asserts, no solo D14-D16) — 6 comparaciones normalizadas:** `D16h` `String(fecha_objetivo)==='2026-12-31'` → `aFechaISO()` (**estaba ROTO**) · `D16h` `String(valor_objetivo)==='30'` → `Number()===30` (pasaba **por suerte**: `String(30)==='30'`) · `E8a-1` `String(ceEst.resumen.nodos)==='2'` → `Number()===2` · `E8a-1` `String(ceEst.nodos_por_dimension.sistema/negocio)` → `Number()` · `E8a-1` `String(ceIdx.nodos)==='2'` → `Number()===2` · `D6` `String(recRow.cerrada_en)!==''` → `aFechaISO()!==''` (pasaba por accidente: `String(Date)!==''`). Los 5 "por suerte" funcionaban con enteros simples pero son la misma bomba con otro tipo. **Verificados como YA correctos** (no se tocaron): `D12` autonomía usa `Number()` ✓ · `D8`/`D15` fechas usan `aFechaISO()` ✓ · `_dirActiva_` maneja boolean de Sheets ✓ · ids/estados/textos son `COLUMNAS_TEXTO` o texto puro ✓. **`selfTestF2_()` (runner ACOTADO):** corre **solo D14+D15+D16** con su propia limpieza, **sin el pipeline pesado** (`correrDirector`/`correrSalud`/briefs/sync ≈ 7 min) → iterar un assert cuesta **segundos**. Los asserts viven en **`_asertsF2_(chk, log)`, cuerpo COMPARTIDO** por los dos runners: duplicarlos garantizaría que diverjan. `selfTest()` completo queda como **certificación final, UNA vez al cerrar**. Verificado que la extracción no perdió nada: **180 llamadas a `chk` antes y después**. **Harness con TIPADO REAL (`sheets_stub.js`):** replica la coerción de Sheets ('YYYY-MM-DD'→Date · numérica→Number · TRUE/FALSE→Boolean · `COLUMNAS_TEXTO`→string intacto) y ejercita `ejecutarCrearObjetivo_` de punta a punta; **`limpiarHostilTexto_` se EXTRAE del archivo real** (una copia en el harness divergiría sin avisar). **h_tipos 16/16, y reproduce D16h offline: el assert viejo FALLA, el nuevo PASA** → esta clase de bug ya no llega al editor. Los 6 harness verdes: **131 checks** (f2 36 · mant 20 · voz 25 · d2 16 · est 18 · tipos 16). **PENDIENTE (Luciano):** correr **`selfTestF2_()`** (segundos, D14+D15+D16); si da TODO OK → **UNA** corrida de `selfTest()` completo como certificación → seguir con `sembrarNorthStarSatori_()` → eyeball `/dev` → `_promote_exec.sh --go`.
- [16-jul · **FIX D15k mal calibrado + BUG REAL de resúmenes apilados** · `_d15_fix_code.sh` → `--go`] **Reportado por Luciano: `selfTest` falló en D15k.** El assert exigía prefijo `"5 tareas estancadas"`; **prod tiene 18 estancadas REALES** → con las 5 de test, `detectarTareasEstancadas` contó **23 (correcto)** y el resumen dijo "23 tareas estancadas…". **El código estaba bien; el assert asumía un entorno limpio.** Arreglado: D15k **computa el esperado de los datos vivos con el MISMO predicado que la función** (activa + no terminal + `fecha_creacion < limite`), captura el número con `/^(\d+) tareas estancadas/` y compara — **cero constantes contra datos de prod**. Mismo tratamiento a D15l (las 3 más viejas se computan de los datos vivos: **son REALES**, más antiguas que las de test a −60d) y a D15j (pasó a verificar el invariante "**exactamente 1 aviso `tarea_estancada` ACTIVO**", no "1 nuevo": si el conteo no cambió, `crearAviso` reusa el existente y no nace ninguno). Barrido de la tanda D14-D16: **D15k era el único con conteo absoluto** (D13e/D14b/D14g son fixtures puras · D14l filtra por un id único de la corrida · D15b-i van contra ids de test o comparaciones pre/post · D16 computa `objAntes`). **★ BUG REAL encontrado al revisar con esa lente (no era solo el assert):** `detectarTareasEstancadas` resolvía por baseline **solo los individuales del formato viejo** (`'Tarea estancada > …'`), **nunca los RESÚMENES anteriores**. Como `crearAviso` dedupea por mensaje EXACTO y el mensaje lleva el conteo, **cada día que cambiara el número nacía un resumen nuevo y el viejo quedaba activo → 1 aviso/día apilado**, justo el ruido que el cambio venía a matar. **Es la misma trampa que cazó la purga del 08-jul con las recomendaciones** (dedupear por texto exacto un texto que muta). **Fix:** se resuelve TODO `tarea_estancada` activo con `mensaje !== msg` **ANTES** de crear → invariante "1 solo resumen activo" **sin churn** (si el conteo no cambió, reusa). Assert nuevo **D15m** (cambiar el conteo → sigue habiendo 1 solo). **Verificado offline: harness 18/18** (pocas→individuales · el límite exacto de `ESTANCADAS_MAX` · conteo real · las 3 más viejas **por fecha, no por orden de hoja** · migración de los individuales viejos · no toca avisos de otro tipo) **y corrido contra el código PRE-FIX: 4 tests fallan** → el harness caza el bug de verdad, no pasa por vacuidad. **EFECTO COLATERAL CONFIRMADO (Luciano lo va a ver en el CM):** la corrida de `selfTest` que falló **ya resolvió por baseline los 18 avisos individuales reales** (quedaron `resuelto`, no borrados — append-only) y la limpieza borró el resumen de test ⇒ **ahora mismo NO hay ningún aviso `tarea_estancada` activo, y el CM se ve vacío en eso**. **NO es pérdida de datos: es el comportamiento diseñado** (el encargo pedía resolver los individuales viejos por baseline). La `corridaDiaria` de las 07:00 lo recrea como **1 resumen "18 tareas estancadas…"**; para verlo ya, correr `detectarTareasEstancadas()` en el editor. **LECCIÓN (regla dura, hermana de la de D2): `selfTest` corre sobre DATOS VIVOS — todo esperado se computa de los mismos datos con el mismo criterio, JAMÁS se hardcodea un conteo.** Un assert con una constante contra prod no prueba el código: prueba el estado de la hoja el día que lo escribiste.
- [16-jul · **FIX asserts stale del brief (D2)** · `_d2_fix_code.sh` → `--go`] **Reportado por Luciano: `selfTest` falló en D2** ("el brief trae BLUF + Las 3 cosas de hoy"). **NO era un bug del brief** — el brief renderizó perfecto (BLUF + `## Hoy` + las 10 secciones + `· contrato v1`, entero en el log del editor). **El assert era STALE:** verificaba el formato viejo que F2 reemplazó por el contrato v1. **Causa: al reescribir `briefDiarioSistema_`/`briefDiarioCliente_` NO se grepeó quién verificaba esa salida.** D2 ahora verifica **el contrato** (BLUF en negrita + `## Hoy` + firma `· contrato v1` + las 10 secciones **en orden**, por **línea exacta**), y cubre **los dos briefs** (sistema Y cliente — el de cliente también cambió). Barrido de una pasada sobre TODO el repo (`Las 3 cosas`, `## Números`, `Qué primero`, `Se auto-resolvió (agentes`, `Señal de KPIs`): **D2 era el ÚNICO stale**; los `## Números`/`## North Star` de D1/D3 apuntan a `estadoVigente()`, otra función que F2 no tocó → intactos. Verificado offline: **harness 16/16** contra la salida real del renderer, con **control negativo** (un brief al que le falta una sección hace fallar D2e → el assert detecta regresiones, no pasa por vacuidad) y el caso `## Cierre` (substring matchea la sección 7; línea exacta apunta a la 10). **LECCIÓN (regla dura): al reescribir el FORMATO de una salida, grep de TODOS los asserts que la verifican — no solo los que tocás.** El síntoma es traicionero: un assert stale se ve igual que un bug de producto, y se paga una corrida de `selfTest` por cada uno que aparece de a uno. Corolario del 08-jul (validar marcadores literales contra el archivo real): **ahora también al revés — validar los verificadores contra el archivo nuevo.**
- [08-jul PM · sesión de desbloqueo] **ABORT del deploy RESUELTO — era un desajuste de mayúsculas en la guarda #3, NO lógica faltante.** El script hacía `grep -q "juicio ANCLADO"` (minúscula) mientras el marcador real vive como `JUICIO ANCLADO` (mayúsc., `18_direccion.js:360`) → falso ABORT case-sensitive. Fix mínimo: `grep -q`→`grep -qi` (1 flag). Verificado con el repo montado: **7/7 pre-condiciones** pasan, `node --check` **5/5** (3 js + scripts de `index.html` y `voz.html`), `bash -n` del script OK, los 9 archivos del `git add` existen, `_capabilities_gen.sh` presente. La lógica A2/B2/D9 se RE-LEYÓ y está completa (`recomendacionDelDia_` devuelve `{texto,kpi,id_cliente,dato}` anclado en las 5 ramas · `aprobacionDesdeRecomendacion` fail-closed + dedupe `rec_id` · 7 asserts D9 sustantivos). Cabo menor: quedó un `.git/index.lock` vacío por `git status` en el sandbox (sandbox no puede tocar `.git/`) — **el propio script lo remueve (líneas 10-13)**. **PRÓXIMO PASO intacto: Luciano corre dry-run→--go (clasp+git = sus credenciales).** Lección: **toda guarda que `grep`ea un marcador literal debe validarse contra el archivo real (mayúsculas incluidas) o usar `-qi`.**
- [08-jul sesión Trillion T1+T2] **Tandas 1 y 2 CONSTRUIDAS por Cowork en working tree** (verificación sintáctica 5/5: `node --check` en 01_schema/18_direccion/09_selftest + scripts extraídos de index.html y voz.html). **T1 (solo cliente, 0 API, sin tocar agente):** C1-delta = aro de estado `#orbRing` sobre el orbe (jade escucha / ámbar pensando con pulso / terracota habla; data ya emitida por `setSato`, reduced-motion respetado, tamaño .84 del canvas en `sizeAll`) · C2 = fila de progreso REAL del turno en el chatlog — `thinking` tras `speaking` en la misma espera = tool GAS en curso (patrón filler 30-jun) → "Consultando el sistema… Ns" con cronómetro real (span aria-hidden, no spamea SR); se limpia al llegar transcripción de Sato / listening / teardown. **T2 (extiende F4/F5, NO reemplaza):** A2 = `recomendacionDelDia_` con JUICIO ANCLADO — cada rama cita el dato (días de la vencida más vieja POR FECHA, integridad+hallazgo, espera de la aprobación más vieja, progreso NS actual/meta) y devuelve `{texto,kpi,id_cliente,dato}`; BLUF del brief sistema anclado; brief cliente con BLUF por KPI en alerta + sección "Señal de KPIs" · B2 = `aprobacionDesdeRecomendacion(recId)` — fail-closed sin tenant (Satori NO es tenant, decisión firme), dedupe por `rec_id` en payload, crea P1 vía `crearAprobacion` + `syncMaestro()` → cola/lote existentes; botón "→ Crear aprobación" en `ccRecRow` solo si `id_cliente` y sin juicio `se_hizo` · columna `id_cliente` en schema Recomendaciones (reconcilia `setup()`) · 7 asserts D9 (A2 determinístico con `pre` inyectado + B2 E2E sobre cliente `__TEST__` + fail-closed) · limpieza extendida (agregadas por cliente `__TEST__`). **`voz/web/voz.html` sincronizado a consultoría** (neutraliza el foot-gun de `ks_pwa_deploy.sh` que copia consultoría→repo). **C3/C4 verificados por lectura de `agent.py` (Tanda 3 cerrada sin dev):** C3 no aplica — INSTRUCCIONES viven en el constructor del Agent y el framework las mantiene por sesión; C4 ya nativo — pipeline LiveKit+ElevenLabs streamea por chunks. **Purga de cierre (08-jul): 1 Alto detectado y parcheado** — el dedupe de `registrarRecomendacionDelDia` era por texto exacto y el texto anclado cambia a diario → habría acumulado 1 rec abierta/día por la misma situación; ahora dedupea por ESENCIA (kpi_objetivo + id_cliente entre abiertas). Falsos positivos descartados con evidencia: `clienteDeProyecto` no lanza (mapa `|| ''`) · `sanitizarCelda` no toca el payload JSON (solo prefija strings que arrancan con `=+-@\t\r\n`). **Correr en GAS = PRÓXIMO PASO** (`_trillion_t12_code.sh`).
- [08-jul 00:30] **F1.1 EN GAS: selfTest TODO OK con D8b** (log de Luciano) — scroll del centro + card Tareas por contextos desplegados. **Eyeball escroleando /dev = pendiente.**
- [08-jul] **Plan de acción Trillion-delta entregado**: `PLAN-ACCION-TRILLION-DELTA-2026-07-08.md` — cruce de los 13 ítems contra el repo post-v11/F1/F1.1. Hallazgos: B4 ⌘K ya existía (cerrado) · C1 medio hecho (`setSato`; delta = solo aro del orbe) · A2/B2 se ENSAMBLAN sobre el lazo F4-F5 (no reemplazan `recomendacionDelDia_`) · A1 con gatillo B8. Tandas: 1=Voz-fluidez (recomendada) · 2=Brief-que-decide · 3=gatillos. **Esperando decisión de Luciano** (tanda + `brief_push_on`).
- [08-jul 00:10] **Tareas-v2 F1 EN GAS: selfTest TODO OK con los 9 D8** (log de Luciano: TAR-0025 creada → RENACE TAR-0026 pendiente +1d → dedupe OK → no-recurrente no renace) · `setup()` reconcilió columnas · `tmp_poblarTipos()` = `{tocadas: 23}` · commit `5e81d27` pusheado (GAS + GitHub). Captura del CM confirma v11 vivo (última corrida `07/07 07:33` sin GMT · stat Salud OK 100%).
- [08-jul] **CAUSA RAÍZ del "no veo Actividad/Calendario" (reporte de Luciano con captura): `#centro{overflow:hidden}`** — el Command Center es un overlay fixed SIN scroll; todo lo full-width de abajo (Actividad, Calendario, y la card Agenda desde @19) quedaba renderizado fuera del viewport, inalcanzable. El mock v10img escroleaba porque era página normal. Fix F1.1 en working tree (ver No verificado).
- [07-jul noche] **CM v11 DESPLEGADO y confirmado por Luciano** ("chequeado e implementada nueva versión", commit `5838677` pusheado, working tree limpio verificado por Cowork).
- [07-jul noche] **Tareas-v2 FASE 1 implementada por Cowork en working tree** (research `docs/TRELLO-a-Satori-mapeo.md` §6 F1): schema Tareas `+tipo +etiquetas +recurrencia +orden` · **`ensureSheet` reconcilia headers ADITIVO** en hojas existentes (fix estructural: antes solo escribía headers en hoja nueva) · `parseQuickAdd` (sigilos `!a #et @cliente ^tipo dd/mm hoy mañana "cada día/semana/quincena/mes"`) y `parseRecurrencia` (1d/1s/2s/1m, clampa fin de mes) **PURAS — harness vm 10/10 casos OK** · `crearTarea`/`crearTareaQuick` (whitelists, @cliente sin proyecto → etiqueta visible, feed) · **hook clon-al-completar en `moverTarea`** (renace pendiente con próxima fecha, dedupe anti re-drag, jamás rompe el drag) · UI board: input quick-add + 7 chips de filtro por tipo (persistentes al re-render) + chips ↻/etiquetas en cards · asserts D8 (9 nuevos) + limpieza extendida (Tareas por descripción `__TEST__`, Actividad por texto) · one-shot `99_tmp_tipos.js` (23 tareas: tipos + reglas reales a las 5 periódicas + limpia "(recurrente)"; ambiguos→admin, confianza 7/10). Evidencia: `node --check` 5/5 + script index (72.8k) parseado + pre-condiciones 9/9. **Correr en GAS = pendiente** (No verificado).
- [07-jul noche] **CM v11 implementado por Cowork en working tree** (pedido de Luciano con video de referencia = mock v10img): (1) calendario semanal REAL en grilla lun-dom + **modal mensual** con navegación ‹ ›/Hoy/Esc — backend nuevo `agendaRango(desde,hasta)` read-only con validación fail-closed; (2) **roster de agentes real** (color por agente como los nodos, última acción del feed, barra "Hoy" = corridas ok/total del día desde la cola ya leída — **sin barra de entrenamiento hasta E8b: dato inexistente, no se inventa**; Director sintético con carga real); (3) **fix GMT**: `fechaHoraCorta` parsea el Date serializado de Sheets → `dd/mm HH:mm` (raíz: Config coacciona ISO a Date); (4) `.stat` armónico (flex centrado, tabular-nums, ellipsis); (5) tablero Tareas con vidrio de card (`glass-bg`) + scrim suave = el cosmos se ve a través. Evidencia: `node --check` OK en los 3 .js + script del index (71k) extraído y parseado OK + 0 residuos de la card Agenda vieja + asserts D7b escritos. **Correr en GAS y eyeball = pendiente** (No verificado).
- [07-jul 17:28] **CIERRE-DEV 100% — selfTest TODO OK post-push con D5/D6/D7** (log de Luciano): D5 FBK-0007 + normalización SI→si · D6 regla (`tareas_vencidas` por TAR-TEST-1, rama esperada) + lazo cierra solo con 2 juicios · D7 AGE-0001 en agendaSemana · limpieza barrió CLI-007 y filas TEST. Commit `679398b` (99_tmp fuera de repo y GAS HEAD, GitHub pusheado, working tree limpio verificado por Cowork). Nota: "18 pestañas" = 17 del schema + `Voz_log` (extra manual, esperada).
- [07-jul] **@19 EN PROD** — evidencia: eyeball de Luciano en `/exec` 16:30 (Agenda + Tareas con las 23 de Trello + lazo F1-F5 + contrato brief + estados de voz). Hoja `Tareas` re-verificada por gviz en la sesión de cierre: 23 filas, headers correctos.
- [07-jul cierre] **Hoja `Feedback` depurada** — evidencia: gviz post-borrado = solo `FBK-0006` (0001-0005 eran clics del loop pre-fix; borradas por Cowork vía Chrome).
- [07-jul cierre] **Hoja `Agenda` descontaminada** — evidencia: gviz post = solo headers, 0 filas (tenía las 23 TAR-* volcadas posicionalmente — ver Imprevistos; 0 eventos reales perdidos).
- [07-jul cierre] **Asserts D5/D6/D7 escritos** en `09_selftest.js` (Feedback/Recomendaciones/Agenda + limpieza extendida en `limpiarTodoTest`) + higiene `index.html` (placeholder honesto del feed + comentario kanban corregido) — evidencia: `node --check` OK ambos archivos. **Correr en GAS = pendiente** (ver No verificado).
- [06-jul] **B3 backup live** — evidencia: `smokeBackup` PASS 6/6 (incl. moveTo drive.file), `backupAhora` 7 copias ok, `drillRestore` eyeball-verificado, trigger `backupSemanal` domingo 04:00. Código en **GitHub privado** `lucianolp22/satori-os` (push OK, off-Mac). `_b5_code.sh --go` → `clasp push OK`.
- [06-jul] **B5 6 fixes live en GAS HEAD** — evidencia: `selfTest()` = **TODO OK** post-push (incl. `E8a-2 SISTEMA (7)` valida el fix #1 O(n²) del cerebro; `F1 BAN-0009` valida #4 nextId; capa Voz verde). Commits `7c1aef4`/`13fab76`.
- [06-jul] **CAPABILITIES.md autogenerado** — evidencia: `bash _capabilities_gen.sh` → 150 líneas, re-generable.
- [06-jul] **Rediseño CM+Voz LIVE en `/dev` + eyeball Luciano OK** — evidencia: commit `20531db` en GAS HEAD (Calidad fuera, `#cmFx` presente, orbe regular, isologo alineado, transiciones ~320ms); Luciano confirmó los 8 cambios en `/dev`. Solo falta promover a `/exec`.

### No verificado
- **FIX órbita avatares (14-jul, commit `a410f76`, en /dev — EYEBALL pendiente, sin promote):** los nodos orbitales del CM que giran alrededor del orbe (Vigía/Analista/Conciliador/Cobrador/Abastecedor/Director) eran HTML estático con glifo SVG fijo — NO los tocaba `agAvatar` en E1.1 B3 (ese cubría órbita legacy `cmRing`/chips/roster, no los `.orbiter` estáticos). Ahora tienen `data-ag=<clave>` y `cmAvataresOrbita()` inyecta `<img>` circular desde `avatar_url` (Config `avatar_<clave>`, misma fuente que la lista de Actividad) con `onerror`→glifo. Cerebro/Bandeja (navegación) intactos; orbe central, aro T1 y progreso del turno sin tocar; borde de cada nodo usa `--nc` (acento). `node --check` OK. **Luciano: recargá `/dev`, mirá que los 6 nodos-agente muestran la imagen (los mismos avatares que la lista de Actividad) sin tapar el aro T1; después promote si OK.**
- **E3 — para que el sync escriba en vivo, faltan 2 pasos de Luciano.** HECHO 14-jul: ✅ selfTest → **D12 verde en runtime real de GAS** (whitelist por caller, escribe CLI-000, refleja np_pausado, autonomía 42.9, títulos re-sanitizados) + **CLI-000 creado** · ✅ **promovido a /exec @24** (rollback @23 en `_promote_rollback.txt`; el label del deploy quedó como "B0.5…" = subject del commit que el propio script crea, cosmético). **PENDIENTE (Luciano):** (1) **Script Property** `OFICINA_SYNC_SECRET` = el valor del reporte de Code (editor Apps Script → Configuración → Propiedades del script; NO va al repo) — el D12 usó un secreto de prueba temporal, el real NO está seteado aún. (2) En el `.env` de la Oficina, cambiar **`GAS_SYNC_URL` a la URL /exec** (misma que `GAS_VOZ_URL`; `/dev` da 404 con Bearer, verificado) → `python3 ov.py sync-cm` escribe CLI-000; comparar vs `/api/v1/estado` por curl. Hasta entonces el push fail-opea (404) sin efecto. Nota: `correrSalud` marcó `cerebro=warn: sin materializar CLI-000` — transitorio, se materializa en la próxima corrida del Director. Y el aviso `voz_acceso_no_autorizado` de hoy es de las pruebas de transporte de Code (POST con secreto inválido a /exec), no una intrusión.
- **E2 — prueba de voz en vivo (Luciano):** pedir a Sato decidir una aprobación → debe REPETIR id+resumen+decisión y esperar "sí"; sin confirmación, CERO llamadas (el gate es conversacional, docstring + N5 v2). Con "sí" → decide y cita el resultado real; con negocio pausado → informa el 423.
- **OV repo (oficina-virtual) — SIN commit (lo commitea Luciano):** `observatorio/app.py` (np_pausado + toggle), `core/config.py` (set_valor), `observatorio/ui/index.html` (btn-np), `observatorio/sync_cm.py` (nuevo), `.env` (gitignoreado, NO commitear).
- **E1.2 T3 — el trigger de 8h NO existe hasta correr el instalador.** El código está en `/dev` (commit `d78aedb`), pero `instalarTriggers()` lo llama **`bootstrap()`**, no `setup()` (ojo: el encargo decía "setup"). **Luciano: correr `instalarTriggers()` (o `bootstrap()`) en el editor GAS** → verificar en Activadores del proyecto que aparece `sincronizarConectores` cada 8 horas (+ siguen `corridaDiaria` 07:00 y `drenarCola` 5min). No requiere promote (los triggers corren sobre HEAD).
- **E1.2 T1/T2 — prueba de voz en vivo pendiente (Luciano):** console/PWA, 5 consultas alternando estado/brief/vehemence/oficina_estado/cliente → el filler SUENA en las lentas y la respuesta llega en el MISMO turno sin re-preguntar; las de Oficina responden directo sin anuncio; "cómo viene Vehemence ahora" trae la aclaración de frescura sin inventar hora. (Conductual, no unit-testeable.)
- **E1.1 BLOQUE B — EYEBALL OK + PROMOVIDO a `/exec` @23** (13-jul, Luciano confirmó eyeball en `/dev` y ordenó el promote; `_promote_exec.sh --go`, rollback a @22 en `_promote_rollback.txt`). Nota: la etiqueta del deploy quedó como la vieja de B0 ("Rediseño CM+Voz… 06-jul") — cosmético, el contenido es E1.1. Seed de las keys nuevas de Config (`voz_url`, `oficina_url`, `avatar_*`): corre en el próximo `setup()`/`sembrarConfig`; hasta entonces `voz_url` cae al hardcode 8787 y el botón Oficina queda oculto (fail-closed, no roto). **D4 (exponer la Oficina a la tailnet)** = decisión de seguridad de Luciano, NO tomada. Avatares en el panel del Observatorio (repo OV) = opcional, fuera de E1.1.
- **Trillion T1+T2 (esta sesión)**: en working tree, NO corridas en GAS (`_trillion_t12_code.sh` pendiente) → `setup()` + selfTest D9 + prueba de voz real + eyeball del botón en `/dev`. La rec abierta de HOY nació sin `id_cliente` (código viejo) → para ver el botón puede hacer falta `registrarRecomendacionDelDia()`; la duplicada vieja se cierra por el lazo (2 juicios). El delta acumulado (v11+F1+F1.1+T1+T2) entra a prod al promover @20 POST-eyeball.
- **F1.1 corrió en GAS (selfTest D8b OK)**; queda solo el **eyeball ESCROLEANDO en `/dev`**.
- Board F1 (quick-add, filtros, chip ↻, drag renace): en GAS HEAD pero **eyeball visual pendiente** (Luciano no llegó a probarlo — el reporte fue sobre lo no-visible).
- Asignación de tipos de la migración: 6 ambiguos → 'admin' (EECC PC, deuda SS, Enred, PC siniestro, Holded, mapear cobros/pagos) — corregibles a mano o con F3; confianza 7/10.
- Transcripción `es-419`+keyterms: aplicada y agente reiniciado (07-jul 11:05); **calidad a validar por Luciano en la próxima conversación de voz**.

## Pendiente
*(re-inventariado en la sesión de cierre 07-jul con `_inventario_cierre.sh` — gate de declaración del CLAUDE.md)*

**Must:**
- Correr `bash _trillion_t12_code.sh` (dry-run → `--go`) + `setup()` + `selfTest()` (D9) + prueba voz (aro+progreso) + eyeball `/dev` (botón lazo + escroleo + board F1) → promover `/exec` @20 (junta v11 + F1 + F1.1 + T1 + T2).
- En ese cierre: borrar `src/99_tmp_tipos.js` (temporal cumplido, patrón 99_tmp). [Hecho previo: `_tareas_f1_code.sh` y `_f11_fix_code.sh` corridos, selfTest D8/D8b OK.]

**Should nuevo (Tareas-v2 F2-F4, con gatillo — doc §6):** F2 recurrencia de PROCESO (`Plantillas_pasos` + `instanciarProceso` + `%_avance` calculado) · F3 panel de detalle de tarjeta + timeline cliente (`guardarTarea`, `esperando_de`, `notas` — cubre también lo único adoptable de Notion, doc `NOTION-a-Satori-mapeo.md`) · F4 bridge Bandeja→crearTarea + auto-"vence hoy" en brief (email-alias pasa por Bastión).

**Should (cabos verificados, con dueño/gatillo):**
- **Archivar 7 deployments GAS viejos** (@4-@14 sin uso; UI de Apps Script → Implementaciones. CUIDADO: no tocar el deployment de `/exec` @19 ni el de `/dev` — verificar IDs contra los Artefactos antes de borrar cada uno).
- **Widget Calendario del CM**: decisión pendiente — la card Agenda (opción A) ya cubre la vista semanal; decidir si un calendario visual se construye o se descarta explícitamente.
- Deuda ARCHITECTURE.md §Pendiente: tokens de sombra dark sin documentar en DESIGN.md · doble escritura de costos (unificar en E3) · agentes lab → flag `activo` (gatillo Forge).
- Sugerencias con gatillo "próximo toque de UI": barras tarea/entrenamiento (dato real o E8b) · onboarding wizard · pase WCAG 2.2 AA · PM dogfooding · higiene tareas programadas · `.venv` fuera del indexador Cerebro · página continuidad de cuenta. (⌘K y barra de coste YA existen — verificado norte v9. Aro de estado del orbe + progreso chatlog = HECHOS en T1, 08-jul.)
- **Tareas-v2 (research Trello listo en `docs/TRELLO-a-Satori-mapeo.md`):** 3 must del doc — alta rápida de tarea desde el board · recurrencia real · etiqueta/contexto. Con gatillo: cuando Tareas vuelva a tocarse.
- **B7 negocio:** SIP al regreso de Nicolás (averiguar fecha + disparador) · Vehemence monto ARS con Micaela (verificar arista fiscal ES/AR con contador) · build-in-public (decisión, gate honesto 4 semanas).
- Bucket B de la purga (PII #6, conector #8-#10) → **B8** con riesgo documentado.

**Nice:**
- Activar `brief_push_on` cuando quieras el brief por email (hoy OFF por diseño) · E8b cuando dispare el gatillo (spec en `docs/INTEGRACION-ENTRENAMIENTO-AGENTES.md`) · Bucket C purga.

**Gates 08-jul:** corrida IG monitor (debe ACUMULAR, no re-baselinear) · subvención Barcelona Activa (decisión).

## Artefactos
| Tipo | Nombre | Ruta / ID / URL |
|---|---|---|
| Proyecto GAS | MAESTRO | scriptId `1M-LYF0GO_Zgh2quGNlCzl4Okcx-DFqQxUhA_jqFqtbJNXYqnIu-2GVnO` |
| Sheet MAESTRO | Satori OS — MAESTRO | `1DMORlkps1Rgvk2D-1XXA7h3R2gMfSGIXirIGR3KjYjk` |
| Deployment prod | /exec | `AKfycbxZJL4E…` (**@19**, 07-jul: P2 F1-F5 + Agenda + Tareas + estados voz) |
| /dev CM | Centro de Mando | `script.google.com/a/macros/satoriconsultoria.com/s/AKfycbzT5QktUHRuKosiuph5rPHU5sZbv2E5E_DNKRVy_6I/dev` |
| Remoto git | GitHub privado | `github.com/lucianolp22/satori-os` (off-Mac backup del código) |
| Backup datos | módulo + carpeta | `src/21_backup.js` → Drive «Satori OS — Backups» (Script Property `BACKUP_FOLDER_ID`) |
| Deploy scripts | CM / B5 / capabilities | `_cm_deploy.sh` · `_b5_code.sh` · `_capabilities_gen.sh` (guardia allowlist + clasp push) |
| Docs B5 | paquete / purga / caps | `PAQUETE-CODE-B5-2026-07-06.md` · `PURGA-SISTEMA-B5-2026-07-06.md` · `CAPABILITIES.md` |
| Runbook | recuperación total | `RUNBOOK-recuperacion-total.md` (el Mac murió + restore + Script Properties) |
| Kit de marca | isologo + variantes | `Projects/KAIROS…/Satori - Identidad Visual/` (lockup horizontal = PNG-embebido+ámbar; usar PNG para recolorear) |
| UI (vanilla GAS) | CM / Voz | `src/index.html` · `voz/web/voz.html` (render-check = eyeball) |

## Desvíos del plan original
- **B4 (rediseño CM) se adelantó a B3** (backup). Ambos cerrados.
- **B6 (motor) mayormente ya existía** (telemetría, estadoVigente, North Star, brief, pipeline Bandeja): re-verificado verde en selfTest → no se reconstruyó; el resto (fase→skill, F2/F3, brief-push) = diferido con gatillo por ser proceso especulativo para un operador solo.
- **Rediseño visual (opción b)** ejecutado ahora (CM header + página de voz completa) en vez de al final.
- Datos+RGPD+go-live AL FINAL (firme).

---

## Apéndice histórico
{Se lee solo si un problema reaparece o se cuestiona una decisión.}

### Decisiones y descartes
- **[06-jul] #7 kill switch = congelar TODO en pausa** (doPost rechaza también lecturas cliente/cerebro, no solo capturar). Decisión Luciano.
- **[06-jul] Bucket B de la purga (PII a LLM + integridad conector) → diferido a B8** con riesgo documentado (corre con datos sembrados/demo hoy). Decisión Luciano.
- **[06-jul] Motor/método B6 (fase→skill, /promote, F2 clasificador general, F3 PM persistente, brief-push) = diferido con gatillo** (equipo/más clientes): proceso especulativo para un operador solo; el 80/20 de B6 (datos/telemetría/estado) ya estaba hecho.
- **[06-jul] O(n²) del cerebro** resuelto con snapshot pre-cargado OPCIONAL (`snap`) en `upsertPorClave_/upsertNodo/upsertArista` — callers viejos sin `snap` = comportamiento idéntico; snap consistente intra-lote (clave repetida actualiza, no duplica).
- **[06-jul] isologo del CM** = PNG del lockup recoloreado negro→crema `#ECEAE3` + ámbar (no hay lockup-reverse; los SVG del lockup son PNG-embebido, no recolorables por fill).
- **[30-jun] `appsscript.json` webapp.access = DOMAIN (Bastión):** NO volver a MYSELF (rompe el CM + URLs de dominio).
- **[30-jun] A' voz = pipeline Deepgram+OpenAI+ElevenLabs+Silero.** VAD Silero = NO se toca (deprecación = warning cosmético). Thinking-sound descartado (tapaba TTS). Mic SIEMPRE Mac (`audioCaptureDefaults`). ElevenLabs Starter pago.
- **[29-jun] Voz A' = LiveKit** (NO stack WS-custom de Kevin, NO OpenAI Realtime). Mic en iframe GAS DESCARTADO (getUserMedia bloqueado) → voz en página local. Token = mint local. Latencia 13s = overhead GAS (no cold-start).
- Decidido (27-jun): kill switch = pausa operativa; alertas email opt-in default OFF. Caller = luciano@ (os@ baja). Descartado: `drive.file` para el token del agente (gate 404 → readonly); SA externa (Workspace la rechaza).

### Imprevistos y resolución
- [07-jul cierre] **Hoja `Agenda` contaminada: las 23 tareas de Trello volcadas POSICIONALMENTE** (descripcion en `hora`, prioridad en `titulo`, estado en `id_cliente`). Invisible en el CM: `agendaSemana()` filtra por fecha y esas filas tenían `fecha=''` → la card se veía vacía y el eyeball de @19 no lo detectó. El importador NO fue (usa `appendFila` por header y apunta a `Tareas` — verificado en código); consistente con un pegado manual en la pestaña equivocada. Fix: Cowork borró las filas 2:24 vía Chrome (headers preservados, 0 filas no-TAR perdidas, `Tareas` intacta con 23; verificación gviz antes y después). Lección: **el eyeball del CM NO verifica las hojas — tras cada corrida que toca datos, gviz de TODAS las hojas tocadas.**
- [07-jul] **Voz MUDA (sin saludo/respuesta): el agente estaba MUERTO desde el 04-jul 09:48** — corte de red/DNS → el worker agotó 16 reintentos contra LiveKit (`failed to connect after 16 attempts`) y KeepAlive NO lo repuso (log sin actividad posterior). El deploy del 06-jul NO tuvo nada que ver. Fix estructural (Cowork, 07-jul): **LaunchAgents versionados** en `voz/launchagents/` (KeepAlive incondicional + ThrottleInterval 15) + **watchdog anti-zombie** (`voz_watchdog.sh`, cada 5 min: si el log muestra muerte terminal posterior al último `registered worker` → `kickstart -k`) + installer `instalar_launchagents.sh` (valida, recarga, verifica; watchdog se carga al FINAL para no matar al agente recién nacido). Lección: **KeepAlive solo no alcanza para workers zombie; los cortes de red largos requieren watchdog por evidencia de log.**
- [07-jul] **Mic inicial = iPhone (flash de Continuity) pese a pickMacMicId** — el `getUserMedia` temporal de permisos era genérico → macOS activaba el default del sistema (iPhone). Fix en `voz.html`: elegir el mic del Mac ANTES de la primera activación (labels ya disponibles si el permiso está persistido) + `deviceId:{exact}` en ese primer getUserMedia + fallback genérico si el id quedó stale. Móvil (IS_TOUCH) intacto. Raíz opcional del lado macOS: desactivar el iPhone como mic de Continuity.
- [06-jul] **`.git/index.lock` huérfano (2 días) tumbaba el commit** → el sandbox de Cowork NO puede tocar `.git/` (`Operation not permitted`) → lo remueve Luciano en el Mac (`rm -f .git/index.lock`); scripts ahora chequean el lock al inicio.
- [06-jul] **guardia `_b3_code.sh` false-positivo sobre `index.html.bak`** (local-only, clasp no lo pushea) → guardia corregida: solo marca `differ` y `Only in <GAS>/src` (ignora `Only in src:`); usa allowlist de los archivos que intencionalmente cambian.
- [06-jul] **GitHub device-flow (`gh auth login`) no llegaba el código** → push vía PAT clásico embebido en la URL (`git push https://user:TOKEN@github.com/...`), saltea keychain/gh. GitHub ya no acepta password para git.
- [06-jul] **imagen pegada en el chat NO se guarda a disco** (solo en contexto) → pedir archivo adjunto o montar la carpeta; el isologo estaba en `Projects/KAIROS…/Satori - Identidad Visual/Exportaciones/PNG/`.
- [06-jul] **CM sirve la versión DESPLEGADA, no HEAD** → cambios de `index.html` se ven en /dev con `clasp push`, pero /exec necesita promover versión (como B4); triggers sí corren HEAD.
- [30-jun] /dev perdió el botón tras /exec = HEAD de GAS desincronizado del repo → regla: SIEMPRE diff repo↔GAS antes de `clasp push` (guardia que aborta).
- [27-jun] El CM NO es auto-screenshoteable (iframe cross-origin GAS) → render-check = eyeball de Luciano.
- [29-jun] `#` inline / paréntesis en comandos pegados rompen el zsh de Luciano → comandos limpios; multi-paso = script desde archivo (`bash x.sh`).

### Changelog del handoff
- **[08-jul] Sesión Cowork — Trillion-delta T1+T2 construidas (working tree):** T1 voz solo-cliente (aro de estado del orbe C1-delta + progreso real del turno en chatlog C2, cronómetro honesto por `lk.agent.state`, sin tocar agente ni VAD) · T2 sobre el lazo F4/F5 (A2 juicio anclado en `recomendacionDelDia_`+briefs con `{id_cliente,dato}`; B2 `aprobacionDesdeRecomendacion` P1 default-deny + botón en `ccRecRow`; columna `id_cliente`; 7 asserts D9; limpieza agregadas por cliente `__TEST__`) · C3/C4 de Tanda 3 cerrados por lectura de `agent.py` (no aplican: instrucciones persistentes + TTS streaming nativo) · `voz.html` sincronizado a consultoría (foot-gun `ks_pwa_deploy.sh` neutralizado) · `_trillion_t12_code.sh` listo (guardias + dry-run/--go). Purga de cierre corrida por Cowork. Pendiente = paso Code (ver PRÓXIMO PASO).
- **[07-jul] Día completo + sesión de cierre (Cowork):** mañana: promoción `/exec` @17 (kill-switch #7 voz) · voz revivida (watchdog anti-zombie + LaunchAgents versionados) · STT `es-419`+keyterms. Mediodía: P2 F1-F3 verificadas en /dev (video) · P2 F4+F5 → **@18** (`selfTest` TODO OK) · B7 marco de cartera (consultoría, con decisiones de Luciano). Tarde: norte v9 contra CM vivo (estados de voz `setSato` + Agenda opción A) · importador Trello 23 tareas → **@19 EN PROD confirmado** · research Trello→Tareas (`docs/TRELLO-a-Satori-mapeo.md`). **Cierre:** asserts D5/D6/D7 + limpieza test extendida · higiene index.html (placeholder feed honesto + comentario kanban) · `Feedback` depurada (solo FBK-0006) · **`Agenda` descontaminada** (imprevisto: 23 TAR-* posicionales — ver Imprevistos) · `_cierre_code.sh` dejado listo (16 guardias; correr = PRÓXIMO PASO). HANDOFF consolidado (párrafos del día → este changelog).
- **[06-jul PM] Sesión Cowork — B0/B0.5 (plan 06-jul):** **CLAUDE.md raíz plantado** (esqueleto 22-jun actualizado contra repo real + 2 reglas nuevas: confirmation-pattern verbal para futuras tools de escritura por voz + auto-plan del agente/AREL formalizada) · **PIPELINE-SatoriOS.md copiado al repo** · **`docs/CRITERIO-arquitectura-agentes.md`** (criterio consolidado de 8+ casos, regla del INDEX cumplida) · **drift-checker CAPABILITIES** (`_hooks/pre-push` + `_install_hooks.sh`; dry-run detectó drift real: `_filaConsumoCore_` de B5 faltaba en CAPABILITIES → se regenera en la promoción) · **HANDOFF consultoría → puntero** al del repo + 69 dumps/one-shot archivados en `_archivo/` (quedan los 5 ks_ operativos de voz + 3 de diagnóstico) · **`_promote_exec.sh`** (dry-run + --go: commit B0.5 + guardia GAS↔repo + promoción `/exec` + verificación + rollback documentado; GAS_VOZ_URL confirma que voz usa el MISMO deployment → 0.1+0.2 en un solo paso). Purga de cierre: 0 críticos / 1 alto (este HANDOFF, resuelto acá) / 5 menores.
- **[06-jul] Sesión Cowork — B3 + B5 cerradas + rediseño CM/Voz:** **B3 backup** (módulo `21_backup.js` = copias semanales a Drive + restore drill probado; código off-Mac en GitHub privado; runbook recuperación). **B5 purga total** (2 auditores adversariales, 0 críticos-hoy) + **CAPABILITIES.md** autogenerado + **6 fixes live** (O(n²) cerebro, guard anti-inyección Bandeja, sanitizar `\t\r\n`, nextId defensivo, lock consumo agentes, kill switch total en pausa) — `selfTest` TODO OK. **Rediseño zen-futurista** coherente CM↔Voz: isologo horizontal embebido, botón voz terracota, Modo calma/luna/«Calidad» removidos, transición dissolve, orbe wireframe regular, header sin solape. DESPLEGADO a `/dev` + eyeball Luciano OK (commit `20531db`, 06-jul); afinado post-eyeball (isologo alineado a «Command Center» + transiciones a ~320ms). PENDIENTE solo promover a `/exec`.
- **[03-jul] Cowork — verificación total + B1/B2:** SOP+template onboarding, paquete B1 higiene, agent.py corregido, VOZ_TOOL_SECRET rotado, marca definida (enso+Alba/Fraunces/Hanken/terracota-jade).
- **[30-jun] A' fase (ii) PWA móvil CERRADA** (Tailscale Serve, sala/identidad únicas anti-zombie) · **[30-jun] A' fase (i) voz grave CERRADA** (pipeline LiveKit) · **[29-jun] Voz integrada al CM CERRADA.**
- [26-jun] Fase Voz local + Purga + Bastión · [25-jun] Integración v14 + PIPELINE.
